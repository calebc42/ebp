# SPEC amendment log

Every normative change to [SPEC.md](SPEC.md) — anything that alters what
a conforming implementation must do — lands as one row here, in the same
commit as the change itself. The row records what changed, which
machine-checked fixtures were regenerated to match, and who reviewed it.

This log is the provenance trail for the frozen spec. Credibility
attaches to the artifacts, not the authorship: an amendment is
trustworthy because a human reviewed it and both reference
implementations' conformance suites re-verified the regenerated
fixtures, regardless of who or what drafted the words.

Rules:

- Wording-only edits (typos, formatting, clarifications that change no
  requirement) need no entry.
- An amendment to the freeze surface (envelope, handshake, §4–§6
  semantics — see the SPEC header) must state in its Change cell why it
  is not wire-breaking, or bump the envelope `v` alongside.
- *Fixtures regenerated* names every regenerated artifact
  (`goldens/widgets.golden`, `goldens/frames.golden`,
  `goldens/hypertext.golden`, `contract.json`) or says `none`. Rows
  predating the extraction of this repo name the old monorepo paths
  (`test/*.golden`, `docs/contract.json`) — same artifacts.
- *Reviewed by* is filled by the human who reviews the amendment before
  it merges to main; a blank cell marks an amendment still in flight.

| # | Date | Section(s) | Change | Fixtures regenerated | Reviewed by |
|---|------------|------------|--------|----------------------|-------------|
| 0 | 2026-07-14 | header | Status → **1.0-rc**: freeze surface declared (envelope §2, handshake §3, §4–§6 semantics; §7–§11 negotiated/optional; §9 node vocabulary additive via §3 negotiation) and this amendment policy instituted. Bookkeeping only — no requirement changed, no wire change. | none | |
| 2 | 2026-07-14 | §5, §9 | Configurable notification action buttons. §9: a notification's `meta.actions` is an ordered array of `{label, on_tap, icon?, dismiss?, input?}` rendered as the platform notification's actions — `dismiss` cancels the notification on tap (Done/Snooze), `input {hint?, key?}` makes the button an inline text reply. §5: `fields` is no longer always `null` — an inline-reply action delivers its typed text there as `{key: text}`. Not wire-breaking: both are additive. `meta.actions` is an unknown meta key to a companion that predates it (it posts no buttons and a body `button` remains the fallback), and `fields` was already part of the `event.action` shape and `null` for every existing interaction, so a client that emits no inline-reply action never receives a non-null value. | none normative — notification `meta` is rendering guidance, not part of the machine-checked node/kind vocabulary in `docs/contract.json`, and no golden carries a notification spec; `docs/contract.json` regenerates only its `api_version` (1.11.0 → 1.12.0, the added builders), not a contract-shape change | |
| 3 | 2026-07-15 | §6, §7 | Companion theme is now a three-way client choice. `theme.set` gains an optional `base` string (`"material"` \| `"default"`), meaningful only when the push carries no `colors`: it forces one of the companion's *own* schemes (Material You, or the built-in default) instead of only clearing the mirror. The reference client's `jetpacs-theme-mode` (`default`/`material`/`emacs`) drives it. Not wire-breaking: `base` is additive and optional, and a companion that predates it sees a `colors`-less push, treats it as the documented clear, and falls back to its own scheme chain — so `base` degrades to "the companion decides." | none — `theme.set` is a raw client→companion send, not part of the machine-checked node/kind vocabulary in `docs/contract.json`, and no golden carries a theme frame; no new builder, so `api_version` is unchanged | |
| 1 | 2026-07-14 | §3, §5, §8, §9 | Schema registry authored (S1): per-node required/optional keys and the frame-kind table now live in `jetpacs-lint.el` and publish as contract_format 2 (`node_schema`/`kind_schema`/`spec_version`). Hand-review of goldens ∪ constructors vs WIDGETS.md/SPEC §9 found the node vocabulary consistent, but several frame *sketches* disagreed with both reference implementations (which agree with each other) — corrected to wire truth, no implementation changed: §5 `event.action` carries `surface`/`revision_seen`/`fields`/`queued_at` (not `revision`; `when_offline`/`dedupe` are queue policy, not echoed); §8 companion→client legs ride `event.action` as actions (not frame kinds), `edit.resync` is `{id, session}` (not `{file, session}`), `diagnostics.show`/`fontify.show` carry load-bearing `seq`, `edit.caret`/`edit.complete` args corrected; §3 welcome also carries informational `protocol`/`server`. Not wire-breaking: every change describes what v1 implementations already emit/accept. | docs/contract.json (format 2) | |
| 4 | 2026-07-14 | §11 | `on_fire` placeholder dynamics ([PLAN-conditions-and-dynamics.md](PLAN-conditions-and-dynamics.md) Phase 1). String values inside an `on_fire` entry's `notify` and `args` (recursively) are interpolated at fire time with `${id}`, `${type}`, `${data.FIELD}`, following §9's snippet-placeholder rules (single pass, unknown/unresolvable tokens stay literal, result always a string; the `cap` name never interpolates). Additive and not wire-breaking: an `on_fire` carrying no `${…}` token is byte-identical on the wire and in behaviour, and the companion is the only interpreter — the tokens ride the existing `triggers.set` shape as ordinary strings. | test/frames.golden | |
| 5 | 2026-07-15 | §10, §11 | Conditions core ([PLAN-conditions-and-dynamics.md](PLAN-conditions-and-dynamics.md) Phase 2). §11: a registration gains the optional `when` state gate — a flat AND-ed array of state predicates guarding the entire fire (no event, no on_fire, no throttle bookkeeping on a failed gate; unevaluable = not holding, fail closed; no OR/nesting/negation), plus the new "State predicates & sampling" subsection (the predicate table incl. the predicate-only `time.window`) and the **normative client rule**: push `when` only when every predicate type appears in the session's new `device.state_types` report, else skip the whole registration — never strip the gate, because a pre-`when` companion ignores unknown keys inside a trigger entry and would arm it ungated. §10: the device report gains `state_types` (under the `triggers` grant) and the catalog gains `state.get {types?, when?}` → `{states, unavailable?, holds?}` (per-type failures land in `unavailable`, never failing the batch; `holds` runs the same evaluator that gates fires). Additive and not wire-breaking: a gate-free `triggers.set` is byte-identical, `state_types`/`state.get` are new welcome/catalog entries old clients ignore, and the client rule keeps `when` off the wire toward companions that predate it. | test/frames.golden; `docs/contract.json` regenerates only its `api_version` (1.17.0 → 1.18.0) — `triggers.set`/`capability.invoke` kind schemas are unchanged | |
| 6 | 2026-07-15 | §11 | Adapter-state types ([PLAN-conditions-and-dynamics.md](PLAN-conditions-and-dynamics.md) Phase 3): `wifi.enabled` and `bluetooth.enabled` join the trigger-type catalog (ENABLED/DISABLED edges only — transitional states are not edges; param/data `{enabled}`) and the state-predicate table (natural state `enabled: true`). Both install-time-permission-only (`ACCESS_WIFI_STATE`; legacy `BLUETOOTH` ≤ API 30); receivers are armed only while a matching registration exists. Additive: new catalog entries negotiate via `device.trigger_types`/`device.state_types` — the client's static batch-1 fallback stays frozen (pinned by `jetpacs-triggers-new-types-not-in-fallback`), so a pre-report companion is never pushed either type. | none | |
| 7 | 2026-07-15 | §10, §11 | `calendar.event` ([PLAN-conditions-and-dynamics.md](PLAN-conditions-and-dynamics.md) Phase 4): both views of the calendar signal. Trigger — `{event?, calendar?, title_contains?}` fires `started`/`ended` edges with zero polling (one ContentObserver + one boundary-parked alarm per registration; the ongoing side persists across process death so a cold boundary alarm still fires the flip). Predicate — `{calendar?, title_contains?}` holds while a matching instance is ongoing; `state.get` samples `{ongoing, title?, end_ms?, next_begin_ms?}`. Runtime `READ_CALENDAR` throughout, fail closed: ungranted registrations skip with a log, the predicate never holds, sampling lands in `unavailable` as `cap-permission`; `device.perms` gains `read_calendar`. Additive: a new catalog type negotiated via `device.trigger_types`/`device.state_types` (frozen batch-1 fallback rule as in #5), and a new perms key old clients ignore. | none | |
| 8 | 2026-07-15 | §10, §11 | Telephony pair ([PLAN-conditions-and-dynamics.md](PLAN-conditions-and-dynamics.md) Phase 5), overriding [PLAN-device-and-surfaces.md](PLAN-device-and-surfaces.md)'s telephony reject for exactly these two. `sms.received {from?, contains?, include_body?}` → `{from, body?}` (edge-only): opt-in fail-closed privacy — `contains` matches the body but `body` rides only under `include_body:true`, content never logged, `policy:"drop"` recommended for body-carrying rules; runtime `RECEIVE_SMS`, multipart concatenated. `call.state {state?, number?, include_number?}` → `{state, number?}` (edge + `{state?}` predicate, default `offhook`): runtime `READ_PHONE_STATE` for the edges, `READ_CALL_LOG` additionally for the number (Android 9+; without it a number-filtered rule never fires); duplicate broadcasts deduped to one fire. `device.perms` gains `receive_sms`/`read_phone_state`/`read_call_log`. Additive: new catalog types negotiated via `device.trigger_types`/`device.state_types` (frozen batch-1 fallback), new perms keys old clients ignore. Play-hostile permissions are accepted cost for an F-Droid-class personal companion (ROADMAP item 10). | none | |
| 9 | 2026-07-15 | §9 (contract.json paragraph); repo extraction | The contract is extracted into this standalone `eabp` repo (SPEC, `contract.json`, `goldens/` — formerly `docs/` and `test/` of the jetpacs monorepo), and `contract.json` renames `api_version` to `reference_api_version` (contract_format 2 → 3): the field describes the elisp reference implementation's Tier-1 API surface at generation time, not the wire — informational only, so the contract reads as implementation-neutral. Not wire-breaking: no frame or node shape changes; `contract.json` is a validation artifact, and the numbers a consumer should pin (`protocol_version`, `spec_version`) are unchanged. Consumers reading `api_version` (none known — the composer reads the vocabulary blocks) follow the rename on their next submodule bump. | contract.json (format 3) | |
| 10 | 2026-07-15 | Title (SPEC, README); repo name | The protocol is renamed **Emacs–Android Bridge Protocol (`eabp`) → Emacs Bridge Protocol (`ebp`)**, and SPEC.md stops titling the protocol "Jetpacs" — Jetpacs is the reference implementation, and per #9 the contract stands implementation-neutral; "Android" in the old expansion contradicted the same goal (any platform that can hold a TCP socket and draw boxes conforms). Naming only — nothing normative changes and nothing on the wire moves: the HMAC domain tag `jetpacs1:` (§3) and the reserved `jetpacs.*` action namespace (§5) are wire constants shared with the reference implementations and are deliberately untouched here (retagging would break pairing; if taken, that is a separate pre-1.0 amendment landed in lockstep with both references). | none | |
| 11 | 2026-07-15 | §3 (freeze surface) | The amendment #10 reserved: the HMAC domain tag is retagged **`jetpacs1:` → `ebp1:`** (`ebp1:client:`/`ebp1:server:`), so the string every conforming implementation keys its mutual proof with names the protocol, not the reference brand. This **is** a breaking change to the §3 handshake — a mixed old/new pair fails MAC verification and cannot connect (cleanly: both sides already fail closed on a bad mac), and existing pairings must re-pair after upgrading. The envelope `v` stays 1 rather than bumping: 1.0 is a release candidate, nothing is published, and no implementation exists outside the two references, which land this in the same lockstep change-set (elisp `jetpacs.el`, Kotlin `JetpacsConnection.kt`/`JetpacsAuth.kt`, and their tests). This is the last planned change to the tag; from 1.0 final it is frozen with the rest of §3 and any future retag bumps `v`. The reserved `jetpacs.*` builtin-action namespace (§5) is unchanged — those builtins belong to the reference pair, not the protocol. | none — no golden carries a handshake frame (MACs are session-dependent) | |
| 12 | 2026-07-16 | §9 | Explicit `key` on lazy-list children ([AUDIT-architecture-vui-vulpea.md](https://github.com/calebc42/jetpacs/blob/main/docs/AUDIT-architecture-vui-vulpea.md) item 1.6 / PLAN-renderer-reconciliation.md K1b/C1). Any `lazy_column` child may carry `key` — a stable string the companion prefers over the child's `id`, then position, as the child's reconciliation identity across pushes, preserving client-side state, scroll anchoring, and item animation under insert/remove/reorder. The reference renderer already implemented this preference order (K1a, landed renderer-only); this amendment makes the attr authored surface: the reference client's `jetpacs-row`/`jetpacs-card`/`jetpacs-list-item` gain `:key`, and `key` joins the common node keys (contract `node_schema."*"`). Additive and not wire-breaking: an absent `key` is byte-identical to the prior shape, and a companion that predates it degrades to id/position keying. | goldens/widgets.golden (three appended cases); contract.json (`node_schema."*"` gains `key`; `reference_api_version` 1.21.0 → 1.22.0) | |
| 13 | 2026-07-16 | §5, §9 | The declarative confirm gate and the authored degrade slot (1.23.0) — the row for contract commit `8786239`, which regenerated fixtures with no amendment entry (a violation of this policy; recorded here after the fact). §5: an action object gains optional `confirm` — a prompt string gating the *client's* dispatch as a native yes/no dialog before the handler runs (declining is a clean no-op; a queued tap confirms at replay). Companion-opaque and **never echoed**: the `event.action` shape is unchanged, and a client must resolve the prompt from its own state (the reference client indexes `confirm` by action name + args at descriptor-build time), never from the delivered frame. The reference client's initial 1.23.0 gate wrongly read `confirm` off the inbound payload and so never fired on-device; the index fix lands in lockstep with this amendment, with a regression test dispatching the companion's exact frame shape. §9: the visualization-ladder nodes `chart`/`canvas`/`month_grid` accept optional `children` as the authored fallback subtree (`jetpacs-additive`), rendered by a pre-ladder companion under §9's unknown-`t`-renders-children rule and ignored by a companion that renders the node. Not wire-breaking despite touching the §5 freeze surface: both are additive optional keys inside existing objects — a companion that predates `confirm` already carries action-object keys it does not interpret (`when_offline`, `dedupe`), and `children` on a ladder node is invisible to a companion that renders it. This entry also repairs the missing row separator that glued amendments #11 and #12 onto one table line. | contract.json (`action_fields` and the action schema optional set gain `confirm`; `chart`/`canvas`/`month_grid` optional gain `children`; `reference_api_version` 1.22.0 → 1.23.0); goldens/widgets.golden (two appended cases) — both regenerated in `8786239`; re-run with this amendment, byte-identical | |
| 14 | 2026-07-18 | §5 | Action-object growth rule (LiveView harvest; jetpacs `slop-docs/LIVEVIEW-harvest.md` §2 / take 1). §5 now states normatively what was folklore: a companion ignores unknown keys on an action object and never echoes them (the delivered `event.action` stays the `kind_schema` shape), and the growth license is bounded — a new plain action-object key must be cosmetic or rate-shaping; a **constraining** key (one an ignoring companion over-acts on) ships only together with its own welcome-announced positive-knowledge report at per-field granularity plus a skip-don't-emit client rule — the #5 `when`/`device.state_types` pattern, which LSP's per-property capability flags mirror at production scale. Not wire-breaking: codifies behavior #13 already relied on ("a companion that predates `confirm` already carries action-object keys it does not interpret"), adds no key and changes no frame; the law binds future amendments, not existing implementations. | none | |
| 15 | 2026-07-18 | §4; §5 contract rider | The ghost fields get their meaning, and the collision rename (LiveView harvest take 3). `surface.update`'s `ttl_s` → **`stale_after_s`**, and the pair's semantics are written for the first time (they existed only as a frame sketch + contract keys): after `stale_after_s` seconds *disconnected* — clock from disconnect, persisted with the cached spec across process death — the companion SHOULD render the surface visibly stale and substitute `stale_spec` (whole-spec, `views` included) when present; cleared at the welcome; absent field = never stale; actions inside a `stale_spec` stay live. Presentation only: `revision_seen` (§5) remains the correctness boundary, and `stale_spec` must not remove dangerous controls (an old companion will not honor the removal). The rename **is** a breaking change to a §4 field name on the freeze surface, taken per the #11 pattern: no normative semantics existed anywhere, no golden carries the frame, `v` stays 1, and the reference pair follows in lockstep — jetpacs main stays pinned to the pre-rename contract until its references (`jetpacs-surfaces.el`, `SurfaceManager.kt`/`SurfaceStore.kt`) follow. Rider: the §5 action-object `ttl_s` (queue expiry — documented since v0, read by the companion) was never registered in the contract's action vocabulary; `action_fields` and `action_schema.remote.optional` gain it. contract.json is hand-edited — its generator (`jetpacs-lint.el`) lives in the pre-fork tree. | contract.json (kind_schema `surface.update` optional: `ttl_s` → `stale_after_s`; `action_fields` + `action_schema.remote.optional` gain `ttl_s`) | |
| 16 | 2026-07-18 | §3, §5, §6 | Offline input survives the gap (LiveView harvest §1's discovered gap: §6 replays only `event.action`, so a queued action's handler read a UI-state store the offline typing never reached — the multi-field-form failure). The welcome gains optional **`input_state`** `{surface: {id: value}}` — the typed-input snapshot, latest value only, delivered *in* the welcome so the client structurally holds it before it can push (the §8 resync philosophy applied to widgets; LSP's client-owns-document-truth contract is the production precedent, its restart re-send being entailed by ownership). §6's replay rule now reads: absorb (revisions + `input_state`, mirrored to the store) → push initial surfaces, which SHOULD reflect absorbed drafts in input nodes' `value` keys so a reconnect push does not wipe an on-device draft → `queue.replay`. `state.changed` gains optional `surface` (ids unique per surface; absent = the old global-`id` shape) — the same context defect, fixed in the same stroke. Not wire-breaking: all three are additive optional keys — an old client ignores `input_state`/`surface` (pre-amendment behavior: drafts lost, ids global), an old companion simply never emits them, and a companion with nothing to report omits `input_state` entirely. | contract.json (kind_schema: `session.welcome` optional gains `input_state`; `state.changed` optional gains `surface`) | |
| 17 | 2026-07-18 | §7, §9 | Granular-styling structural core (jetpacs `slop-docs/AUDIT-ui-expressiveness.md`, buckets 2/3/5). §9 gains **universal box-model attributes** on any node — `pad` (per-side object; the scalar `padding` keeps its meaning, `pad` wins where both appear), `width`/`height`/`min_`/`max_` variants, `fill_fraction`, `aspect_ratio`, `bg`, `corner` (dp or per-corner; the one shape `bg`/`border`/`clip` share; numeric wins over surface's `shape`, whose `circle` survives), `border`, `alpha`, `clip`, and child `align_self` — application order corner → clip → bg → border; **container granularity** — row/column/flow_row `arrange` main-axis enum (`start`/`center`/`end`/`space_between`/`space_around`/`space_evenly`, precedence over `spacing` when not `start`), row `align` gains `baseline`, flow_row `align`, lazy_column `spacing` + `content_padding`; and the normative **color-value rule** — role-or-hex everywhere (3/4/6/8-digit hex), superseding the span hex-only wording (pure widening). §7 documents the pushed role palette for the first time and it gains `success`/`warning` (companion falls back to its built-in pair when unpushed). De-facto enums are recorded normatively from the goldens and the reference renderer so new values finally have a compat story: text `style` body/title/headline/caption/label/mono; button `variant` filled/tonal/outlined/text; progress `variant` circular/linear with value-absent = indeterminate; surface `shape` rounded/rounded_small/circle; box `alignment` = the nine top/center/bottom × start/center/end compounds, unknown → `top_start`. Not wire-breaking: every new key is additive-cosmetic under §9's unknown-key rule (an old companion renders the prior look, content intact); enum documentation records existing wire truth; the color rule accepts strictly more than before. | contract.json (node_schema `"*"` gains the box-model set incl. `align_self`; row/column gain `arrange`; flow_row gains `arrange`/`align`; lazy_column gains `spacing`/`content_padding`) | |
| 18 | 2026-07-18 | §3, §9 | `enabled` on input controls — the first **constraining** key shipped under §5's growth rule, together with its bespoke channel. §9: any Input-family node may carry `enabled` (default true; false renders the platform disabled affordance and suppresses every dispatch; `editor` keeps its own `read_only`). §3: the welcome's new `can_disable: true` is the positive-knowledge report licensing it, with the normative skip-don't-emit client rule — toward a welcome without it a client omits the key entirely and, where the disabled state is load-bearing, omits the control's action instead (an action-less control is inert on every companion). Not wire-breaking: `can_disable` is an additive welcome key an old client ignores, and the client rule keeps `enabled` off the wire toward companions that predate it — the same shape as the `when`/`device.state_types` amendment (#5). | contract.json (kind_schema `session.welcome` optional gains `can_disable`; `enabled` joins the optional set of menu, button, icon_button, chip, assist_chip, text_input, checkbox, switch, enum_list, date_button, time_button, slider) | |
| 19 | 2026-07-18 | §5, §10, §11 | Manual triggers (Easer harvest, jetpacs `docs/PLAN-easer-adoptions.md` item C — Easer's widget-as-trigger generalized, Tasker task-shortcut parity). §11 gains the `manual` trigger type: no params, fire data `{source}` (`tap` \| `emacs`), nothing armed — it fires only when explicitly invoked, and a removed row cannot fire (replace-set). §5 gains the `trigger.fire` builtin (`{builtin, id}`): the on-device invocation that runs the full pipeline (gate, throttle, `on_fire`, event/queue) with `{source: "tap"}` — Emacs-dead by design, so pinned shortcuts, QS tiles, and notification buttons become rule buttons; `shortcut.pin` is the pairing. §10 gains the `trigger.fire` capability (`{id}`), the Emacs-initiated twin with `{source: "emacs"}`; unknown or non-`manual` id → `cap-failed`. Deliberately one letter from `trigger.fired` (cause vs effect) — called out in prose. Not wire-breaking: a new builtin is additive under §5 (a companion that predates it drops the unregistered builtin; a client gates on the welcome's `trigger_types` containing `manual` before authoring any of it), and new catalog entries negotiate by presence like every type since #5. | contract.json (`action_schema` gains the `trigger.fire` builtin row: required `builtin`, `id`) | |
| 20 | 2026-07-18 | §11 | `state.edge` — the level→edge bridge (Easer harvest item E: Easer's entire Event/Condition/`condition_event` machinery collapsed into one trigger type over the existing predicate vocabulary). A registration `{type: "state.edge", params: {when: [predicates…], edge: rise\|fall\|both}}` fires when its ANDed conjunction's truth flips in the declared direction (`rise` = false→true, default); fire data `{holds, edge}`. `params.when` is the *exact* §11 `when` vocabulary (same validation and evaluation — the vocabularies cannot fork); trackable subset = `state_types` − {`time.window`, `calendar.event`}, both zero-cost exclusions; out-of-subset rows reject the whole set. Arm-time evaluation seeds silently (no fires on re-arm/reboot; missed flips self-heal at the next driving event). Row-level `when` stays legal as an additional fire-time gate; duplicate predicate types across the two draw a client warning. Normative client rule: skip the whole row unless every tracked type appears in `device.state_types` (the `when`-strip rationale). Named `state.edge`, never `state.changed` (§5's widget-input kind — a permanent grep/log footgun avoided). Not wire-breaking: a new catalog type negotiated via `device.trigger_types` by presence, exactly the #5-#8 pattern; a companion that predates it is never pushed one. | none — the type negotiates via the welcome; no golden added (the goldens' generator lives in the pre-fork tree — fixture coverage awaits the rebuild's own generator) | |
