| 34 | 2026-07-22 | §7.2, §6.2, §24.2 | **The jsonrpc.el floor, restored.** The llm-poc-2 rewrite drifted from decision log #2 (slop-docs/JSONRPC-conversion-kit.md: "the Emacs side uses core `jsonrpc.el` unmodified — for transport, framing, and id bookkeeping"); two normalization rules contradicted that floor and are corrected. (1) §7.2: request IDs are a JSON string **or a safe integer** (string form keeps the §4.4 grammar and 64-octet cap; `null` and fractional numbers stay prohibited) — jsonrpc.el's sequential integer IDs conform without adaptation; the string-only rule was an over-tightening with no design provenance. (2) §6.2 gains a scoping paragraph: the receiver strictness of §6.2 and §4.1 (duplicate members, strict UTF-8, header caps, the close taxonomy) is REQUIRED for the Companion — the accepting, adversarially-exposed side — and RECOMMENDED for the Emacs endpoint, which dials only its authenticated Companion and MAY delegate framing to a host JSON-RPC library, accepting its tolerances; §6.1 sender obligations are unchanged. §24.2 cross-references the scoping. Not wire-breaking: every previously valid frame remains valid; integer IDs were already legal JSON-RPC and the poc-v1 line accepted them. | goldens/frames.golden (one integer-id frame appended); validate.py id check | Caleb Christensen |
| 35 | 2026-07-22 | §4.5, §24.6 (cross-ref §6.2) | **The depth-64 response, pinned.** §4.5 named a 64-container JSON nesting limit but never fixed the wire response — or the ordering vs. parsing — when a body exceeds it, so a naive receiver could hand a 65-deep body to a recursive parser and overflow its stack before any check ran. Both llm-poc-2 twins (Kotlin `FrameCodec.exceedsDepthLimit`, elisp `ebp--exceeds-depth-p`) already resolve this by a linear pre-parse depth scan that rejects a past-64 body as a §6.2 Parse Error (`-32700`, `id: null`, MAY continue). §4.5 now REQUIRES that response and its ordering-before-parse, and cross-references §6.2; the bounded-stack determination is scoped REQUIRED-for-Companion / RECOMMENDED-for-Emacs to match amendment #34's §6.2 receiver-strictness carve-out (Emacs MAY delegate decoding to jsonrpc.el). §24.6 item 3 adds over-deep JSON nesting to the required adversarial vectors. Additive and not wire-breaking: a 65-deep body was undefined before, so no previously valid frame is invalidated; the limit value (`limits.fixed.max_json_depth`) and the `-32700` parse-error class already exist in the contract, so no contract edit is needed. | goldens/wire/21-deep-nesting.bin (65-deep body, expect_error `parse-error`) + manifest.json entry; validate.py reference decoder gains the pre-parse depth scan (MAX_DEPTH from limits.fixed.max_json_depth) |  |
| 36 | 2026-07-22 | §18.4 | **Theme polarity: follow-system (W1).** §18.4's `dark` was a boolean with no way to defer to the device's light/dark setting, so Emacs could force light or dark but not follow the OS. Omitting `dark` now means follow-system; with `colors` (an Emacs-theme mirror, or `null` for the Companion's native scheme) this expresses the light / dark / follow-system / mirror-Emacs choices the theme picker needs, without EBP naming a platform theme system. The mode choice is Emacs-side policy; the wire carries only the resulting `theme.set`. Additive and not wire-breaking: `dark` is already optional in the contract and omitting it was previously undefined; no member or enum is added, so no contract or golden change. | none (prose clarification of an existing optional member) |  |
| 37 | 2026-07-22 | §17.7 | **Toolbar structural line-ops (W3).** §17.7's operations (`snippet`/`on_tap`/`menu`/`command`) had no Companion-local structural edit, and `command` is valid only for a synchronized editor in `READY` — so an app in the local-editor tier (orgseq) had no promote/demote/move gesture. A new `line` operation (`promote`/`demote`/`move-up`/`move-down`) is a Companion-local single-undo edit of the cursor's line, valid in every editor tier; in a synchronized editor it flows through §19 like any other local edit, and an app needing a semantic Emacs operation uses `command`. promote/demote adjust outline depth (`*` heading markers / list indent), move swaps adjacent lines. Additive: `line` is a new optional primary op; no previously-valid frame carried it. `toolbar.ops` gains `line` and a closed `toolbar.line_ops` vocabulary for the generated linters. | contract toolbar.ops/line_ops; validate.py green |  |
| 38 | 2026-07-22 | §6.2, §8 | **Pre-close frame-too-large diagnostic (W5).** §6.2 closed silently on a size-cap framing fault, so a peer never learned why (the retired poc-v1 `1400`). When the close is triggered by a complete, well-formed header section (oversized `Content-Length`, or an over-cap header section) — stream synchronized, cause unambiguous — a SHOULD-level `log.error` with the new diagnostic-only code `1400 frame-too-large` is emitted before the close, using §22.3's shape and rate limit, scoped to authenticated states (`SYNCING`/`READY`) and never on a desynchronized or ambiguous fault. `1400` is diagnostic-only — carried only in a `log.error` param, never returned as a response `error.code`; §8's preamble now records that category. Additive: the MUST-close is unchanged and silent close stays conformant (every wire golden's `expect_error: close` holds). | contract error_codes[1400] + §8 row (drift-checked pair); validate.py green |  |
| 39 | 2026-07-22 | §13.1, §10.2, §22.1 | **The `tile:*` surface namespace (W6).** §13.1 listed only app/notification/widget; the reference companion's host Quick-Settings tile slots (`tile:*`) had no home. `tile:<name>` is registered as a first-class capability-gated surface namespace, gated on a new `surfaces.tile` protocol capability, exactly parallel to notification/widget: §13's revision/tombstone/limit/update/remove rules apply unchanged, §10.2 adds `tile` to the applicable-target and profile-required clauses, and §22.1 registers `surfaces.tile`. Additive: under §13.1's positive-knowledge model an unlisted namespace was already unsupported, so this only widens what a granting companion MAY accept; the core three namespaces and every existing capability are unchanged. | contract capabilities += surfaces.tile; validate.py green |  |
| 40 | 2026-07-23 | §4.5, §20.1 | **A numeric bound on the device report.** §4.5 already requires the welcome reservation to count "the complete device report" at "the largest state the implementation can report", but with no numeric limit that worst case is implementation-defined and the always-fits-`max_frame_bytes` guarantee is unprovable at the wire. A new `max_device_report_bytes` limit (REQUIRED when `capabilities` or `triggers` is granted) bounds the report's canonical size; §20.1 requires the report to fit it (advertise a bounded subset, never depend on truncation) and §4.5 counts it there. Additive: the reservation already covered the report, so no previously-conformant Companion is invalidated. | contract `limits` += `max_device_report_bytes`; validate.py green (limits are not spec-synced, no golden change) |  |
| 41 | 2026-07-23 | §18.1 | **`dialog.style` fallback pinned.** §18.1 lists `style` as `dialog`/`sheet`/`sheet_full` but never defined an unrecognized value, while §12 rule 6 requires a closed-vocabulary member to define a safe fallback or whole-object rejection. §18.1 now requires an unknown `style` to fall back to `dialog` (parallel to `text.style`'s body fallback); a Companion MUST NOT reject a dialog for an unknown `style` alone. Additive prose: `style` is already an optional closed vocabulary, so no member, enum, or golden change. | none (prose; contract `dialog.style` unchanged) |  |
| 42 | 2026-07-23 | §21.4 | **Source-to-sink approval is per combination.** §21.4 already required "approval of that source-to-sink combination", but the wording admitted a single blanket allowance. A clarifying sentence states the approval is per (sensitive source type, sink kind), defaults to deny, and that a global boolean covering all sources or all sinks is non-conformant — steering implementers to a set-of-approved-pairs model. Prose clarification of existing text; no contract or golden change. | none (prose) |  |
| 43 | 2026-07-23 | §18.6 | **Reminder boot re-arm (non-normative).** Implementations read §18.6's "persist the accepted set across process and device restarts" as satisfied by a durable store alone, while the platform clears scheduled alarms at reboot. A non-normative note records that persistence alone is insufficient — the Companion must re-arm platform alarms for unfired reminders from the persisted set after a device restart, or the surviving registrations never fire. Non-normative note only; no normative, contract, or golden change. | none (non-normative note) |  |
| 44 | 2026-07-23 | §21.1, §5.2 | **The device-lifetime firing authority.** §21.2 mandates firing with no `READY` session and §21.1 mandates persistence across restarts, but the spec named `triggers`/`reminders.owner` only as *session* capabilities (§22.1) with re-check "at trigger-arm time" (§20.1), and §5.2 modelled only sessions — so a careful implementer could read "no session ⇒ capability not granted ⇒ do not fire," contradicting §21.2. §21.1 now states firing, durable admission, `on_fire`, and reminder presentation are device-lifetime obligations authorized by the persisted registration itself (in force until an empty replace-set, pairing revocation, or OS-permission withdrawal), not by an active session's negotiation; a later session that does not re-grant `triggers` MUST NOT disarm prior registrations, and supersession MUST NOT interrupt firing. §5.2 adds the parallel sentence. Clarifying prose; no contract, enum, or golden change (the always-fire-without-session behavior was already required by §21.2). | none (prose) |  |
| 45 | 2026-07-23 | §21.5 | **Catalog fire-data is REQUIRED-present; `time.precision` pinned.** The §21.5 catalog's Fire-data column listed each type's members but no clause made them required in the emitted `args.data`, and the only `precision` sentence was a MUST-**NOT** (don't over-claim), trivially met by omission — so a conformant Companion could fire `time` with no `precision` and `timezone.changed` with no `tz`, leaving a consumer that reads them nothing. §21.5 now requires every Fire-data member not marked optional (`?`) to be present in the frozen fire data (§21.2), and requires `time.precision` present and `inexact` unless delivered on an exact-alarm facility within an advertised bound. Clarifying prose; the catalog columns already named the members, so no contract, enum, or golden change. | none (prose) |  |
| 46 | 2026-07-23 | §21.5 | **`every_s` coalescing: when it fires and the resumed phase.** §21.5 required missed intervals be "coalesced into at most one occurrence" with "no catch-up burst," but "at most one" permits zero-or-one and the text never said *when* the coalesced occurrence fires or how the cadence re-phases — so three conformant impls could fire once immediately, wait for the next boundary, or re-phase from the fire instant, and a counting/timing handler would see all three differ. §21.5 now requires the coalesced occurrence be admitted once, promptly, at restore/re-arm time (not deferred, not skipped) and the schedule resume on the original `anchor + k·interval` grid without re-phasing to its delivery instant. Clarifying prose; no contract or golden change. | none (prose) |  |
| 47 | 2026-07-23 | §21.5 | **`time.at_ms` clock semantics.** §21.5 constrained only the acceptance-time comparison ("later than the wall clock at set acceptance") and said nothing about which clock a *pending* one-shot fires against or its behavior under an NTP/manual clock change — unlike §21.2, which protects throttle against rollback. §21.5 now states `at_ms` names an absolute wall-clock instant, a pending one-shot fires when the effective wall clock (§15.2) first reaches it, a forward jump past `at_ms` fires it promptly, and a backward change neither retracts a completed one-shot nor advances an unfired one earlier than `at_ms`. Clarifying prose; no contract or golden change. | none (prose) |  |
| 48 | 2026-07-23 | §21.5 | **Sensitive-payload encryption boundary.** §21.5's "if queued, payloads MUST remain in app-private encrypted storage" left the encryption boundary undefined (whole record? fire data? captured fields?) and never acknowledged the single-durable-queue design in which a plaintext queue holds one sensitive record. §21.5 now names the boundary — a queued `sms.received`/`call.state` record's fire data and captured sensitive fields MUST be keystore-encrypted before the §21.2 commit and decryptable only for delivery/deletion — makes it unconditional where a keystore facility exists, requires a Companion that cannot meet it to NOT advertise the source, and permits non-sensitive records in the same queue to stay plaintext. Clarifying prose; no contract or golden change. | none (prose) |  |
| 49 | 2026-07-23 | §18.6 | **Reminders are scoped to the pairing identity.** §18.6 scoped reminders to `owner` but — unlike triggers (§21.1 "for the pairing identity"), the queue (§15.1 "per-pairing"), and shortcuts (§20.3) — never stated they are partitioned by pairing identity, though §9.1 revocation must erase them; a flat owner-keyed store would let two pairings' owner sets collide and defeat per-identity erase. §18.6 now states a reminder owner set is scoped to the pairing identity (one pairing's sets/receipts/alarms invisible to another), the durable store MUST be partitioned by identity so §9.1 erases exactly that identity's reminders, and reminder presentation is a device-lifetime obligation like trigger firing. Clarifying prose; no contract or golden change. | none (prose) |  |
| 50 | 2026-07-23 | §21.5 | **`boot` requires a durable generation gate to advertise.** §21.5 offered "a stable platform boot generation OR an equivalent durable boot marker" but never said what to do when neither is obtainable, nor whether `boot` may be advertised without one — inviting a fallback that relies on the boot signal arriving exactly once, which double-fires on a redelivered broadcast (the exact case generation-gating prevents). §21.5 now requires: neither obtainable ⇒ MUST NOT advertise `boot`; and the delivery of a single boot signal MUST NOT substitute for a durable generation gate, so at-most-once-per-boot holds even if the signal is redelivered. Clarifying prose; no contract or golden change. | none (prose) |  |
| 51 | 2026-07-23 | §17.2 | **Unadvertised image URI form is content-invalid.** §17.2 imposed a sender gate ("Emacs MUST use only an advertised form"; the Companion MUST advertise at least one of `image.https`/`image.data`) but never stated the RECEIVER's duty when a well-formed but unadvertised form arrives — a `data:image` when only `image.https` is in that profile's `features`, or vice versa. §17.2 now resolves it as a validation-layer rule parallel to §16.2/§17.1: an image whose URI form is not advertised in the applicable target's `features` is content-invalid and MUST be rejected with `1201`, not silently loaded. A validator keyed on the full vocabulary MUST therefore gate the image-form check on the advertised feature set. The reference companion advertises both forms in every image-bearing profile, so no unadvertised form is reachable from a conforming Emacs; a single-form profile would apply the gate. Clarifying prose; no contract or golden change. | none (prose) |  |
| 52 | 2026-07-23 | §17.2 | **`image.data` supported-format set and "active" defined.** §17.2 required the Companion to "reject active or unsupported formats" for `image.data` but named neither the supported raster set nor "active", so every implementer invents both. §17.2 now defines "active" as any format capable of scripting, external reference resolution, or code execution (notably `image/svg+xml`), requires those be rejected before decode, and names a minimum mandatory raster set — `image/png` and `image/jpeg` — that a Companion advertising `image.data` MUST decode, with `image/gif`, `image/webp`, `image/bmp`, and `image/heic`/`heif` OPTIONAL. Clarifying prose; the reference decodes {png, jpeg, gif, webp, bmp, heic, heif} and rejects svg. No golden change; the accepted-type set is projected into `contract.json` as `image_media_types` for shared tooling. | contract `image_media_types` (informational); validate.py green |  |
| 53 | 2026-07-23 | §16.2, §17.1 | **A spec-defined but unadvertised node type degrades like an unknown type.** §16.2 described the degrade path only for an "unknown node type", while §17.1 said an unadvertised type "MUST be treated as unsupported even if it appears in this section" — colliding for a type the Companion fully implements but Emacs did not advertise in this profile (strict-validate-and-1201, degrade, or ignore?). §16.2 now states unambiguously, at the validation layer, that a known type absent from the applicable `node_types` is treated EXACTLY as an unknown type — its subtree is scanned for nested valid nodes, but its per-type schema is not applied, it registers no stateful draft, and it dispatches nothing; §16.1's strict-reject rule applies only to advertised/core types. §17.1 cross-references. A validator keyed on the full vocabulary MUST gate per-type rules on the advertised `node_types`. Clarifying prose; no contract or golden change. | none (prose) |  |
| 54 | 2026-07-23 | §17.5 | **`chart` x-axis is ordinal.** §17.5 required each ChartPoint to carry finite numeric `x`/`y` but never said how `x` maps to a horizontal position (value-scaled vs. index-ordinal) or whether points are sorted, so a sender could not predict the plot. §17.5 now states the x-axis is ORDINAL: points render evenly spaced by array index within their series, and `x` is presentation/accessibility data and the tap payload, NOT a scaled coordinate. A value-scaled axis is out of scope for `chart`; an app needing one uses `canvas`. Clarifying prose matching the reference (`xLine(i)=w*i/(n-1)`); no contract or golden change. | none (prose) |  |
| 55 | 2026-07-23 | §17.5 | **`chart.on_point_tap` resolution defined.** §17.5's "return the complete authored point object" was singular but a chart may have multiple series of unequal length and a tap rarely lands on a vertex, leaving which series and how a between/off-point tap resolves undefined. §17.5 now states the Companion snaps the tap to the nearest authored point BY ORDINAL INDEX of the FIRST series (index clamped to that series' length); there is no between-points null result. Clarifying prose matching the reference; no contract or golden change. | none (prose) |  |
| 56 | 2026-07-23 | §18.4 | **A missing on-color is derived from the pushed role.** §18.4 requires merging missing roles with "a legible platform fallback", but a theme that pushes `primary` (or `surface`/`background`/…) WITHOUT its paired `on_primary` yields a low-contrast pair if the Companion adopts its base scheme's on-color against the foreign pushed color. §18.4 now requires that when a role is present but its paired on-color is absent, the Companion derive a contrast-legible on-color FROM THE PUSHED COLOR (as already mandated in effect for `success`/`warning`), not adopt the base on-color. Impl conformed: `buildColorScheme` now derives a legible on-color for every missing on-role whose base role was pushed. Clarifying prose; no contract or golden change. | none (prose) |  |
| 57 | 2026-07-23 | §18.4 | **Standard syntax-role set enumerated.** §18.4 defined `syntax` only as a "Syntax-role to SyntaxStyle map" and never named the role vocabulary — asymmetric with theme colors, which §18.4 enumerates in prose and `contract.json` projects as `theme_roles`. §18.4 now lists a standard syntax-role set (`comment`, `string`, `keyword`, `function`, `constant`, `variable`, `type`, `number`, `operator`, `preprocessor`, `heading`, `link`, `todo`, `done`, `tag`), states unknown roles are ignored, and projects it into `contract.json` as `syntax_roles` so both endpoints and conformance tooling share one vocabulary. Additive: `syntax` role keys were previously unconstrained, so no previously-valid theme is invalidated. | contract `syntax_roles`; validate.py green |  |
| 58 | 2026-07-23 | §18.4 | **SyntaxStyle member precedence, and non-`fg` members are advisory.** §18.4 lists a SyntaxStyle's `fg`/`bg`/`font_weight`/`italic`/`underline` but never said how they compose with the Companion's own per-token rendering (it may draw comments italic / keywords bold on its own). §18.4 now defines precedence: a SyntaxStyle's PRESENT members override the Companion's intrinsic styling for that role (each independently) and ABSENT members fall back to the Companion default; and it states a Companion MAY honor only `fg` and treat `bg`/`font_weight`/`italic`/`underline` as advisory where its highlighter cannot express them per-role. Clarifying prose matching the reference (fg-honored, others advisory); no contract or golden change. | none (prose) |  |
| 59 | 2026-07-23 | §18.5 | **Notification `priority` is best-effort and monotonic; platforms MAY coalesce.** §18.5 lists five priorities (`min`/`low`/`default`/`high`/`max`) but never said whether they must be mutually distinguishable, while a common platform (Android channel importance) has no user-facing level above `high`. §18.5 now states `priority` is a best-effort hint: a Companion MUST present it monotonically (never below the requested level relative to `default`) and MAY coalesce adjacent levels — notably `high` and `max` — where the OS lacks a distinct level; authors MUST NOT rely on a five-way distinction. Clarifying prose matching the reference (high/max→one importance); no contract or golden change. | none (prose) |  |
| 60 | 2026-07-23 | §18.5 | **`chronometer.count_down` semantics pinned.** §18.5 defined chronometer as `{base_ms, count_down?}` with `base_ms` "an epoch timestamp" but never said what `count_down` does relative to `base_ms` — count-up conventionally elapses from a PAST base, count-down remains to a FUTURE target, and the field meant opposite things. §18.5 now states `count_down:false` counts elapsed time forward from `base_ms` (which SHOULD be in the past) and `count_down:true` counts remaining time down to `base_ms` (which SHOULD be in the future); when `base_ms` is on the wrong side of now the Companion presents zero-or-elapsed rather than a negative timer. Clarifying prose; no contract or golden change. | none (prose) |  |
| 61 | 2026-07-23 | §17.7 | **At most one `${input:…}` prompt per snippet.** §17.7 described `${input:Prompt}` as "One local free-text prompt" but a snippet may syntactically contain several with different titles, undefined as to prompt count/order/value reuse. §17.7 now restricts a snippet to AT MOST ONE `${input:…}` token; a snippet with more than one is an invalid toolbar item. Clarifying prose matching the reference (prompts once on the first token, reuses that value); no contract or golden change. | none (prose) |  |
| 62 | 2026-07-23 | §17.7 | **`line-start` no-op is the exact literal prefix.** §17.7's "no-op when the line already starts with the exact literal inserted prefix" read as byte-for-byte, but an implementation could (and the reference initially did) compare whitespace-trimmed, so a `"- "` prefix wrongly no-op'd on an indented `"  - item"`. §17.7 now states the already-present test is a byte-for-byte comparison of the inserted prefix against the start of the caret's line (no trimming): a prefix carrying leading whitespace is matched literally, and an indented line whose non-space content matches is NOT treated as already-present. Clarifying prose; impl conformed (`regionMatches` at the line start, no `trimStart`). No contract or golden change. | none (prose) |  |
| 63 | 2026-07-23 | §16.5 (cross-ref §16.1) | **`id` added to the universal-attribute table.** §16.5's "Universal node attributes" table enumerated `key` but omitted `id`, though §16.1 establishes `id` as a node's fallback presentation identity that any node MAY carry ("its `key` when present, otherwise its `id` when present") and `contract.json`'s `universal_node_attributes` and `validate.py`'s `UNIVERSAL` set already include it — an inclusion that is load-bearing, since it is the only reason a validator keyed on per-type schemas accepts a §16.1-legal `id` on a Core-Set node (e.g. `text`, whose `node_schema` row omits `id`). An implementer deriving the universal set from §16.5 alone would omit `id` and wrongly reject that `id`. §16.5 now carries an `id` row (identifier; document-unique node identity per §16.1). Editorial/clarifying: it documents already-normative behavior; no member is added to the wire vocabulary, no previously-valid frame is invalidated, and `contract.json` already carries `id`, so no contract or golden change. | none (prose; `contract.json` `universal_node_attributes` already includes `id`). Tooling follow-ups (not spec changes): `validate.py` under-enforces two existing requirements — it gates an action name only on the presence of a dot, not the full §4.4 namespaced-identifier grammar (§14.1), and checks `ttl_s` presence but not its §14.1 `integer 1..604800` domain; both are tracked for a reference-validator pass. |  |
| 64 | 2026-07-23 | §17.1, §17.2 (contract) | **icon `name` is a string; the contract projection is corrected.** §17.2 types the icon node's required member as `name: string`, and §17.1's identifier-field list (`icon`, `syntax`, `id`, `key`) deliberately omits `name` — two independent SPEC signals that `name` is a plain string. `contract.json` `field_types` projected `name` as `identifier`, the lone artifact treating it as a §4.4 identifier. Per §2.2 the specification is authoritative and the projection is the defect: `contract.json` `field_types.name` is corrected to `string`. An icon name that is not a §4.4 identifier (a display glyph name, or a name with a space) is valid input, and §17.2's "An unresolved name MUST render a harmless placeholder or nothing" already governs a name the catalog does not resolve. No SPEC prose change and no golden change (every golden icon name is also a valid string); `validate.py` does not gate `field_types` values. | contract.json `field_types.name` -> `string` |  |
| 65 | 2026-07-24 | §17.4, §17.1 | **icon_button `icon` is an identifier — the §17.4 row is aligned.** The §17.4 icon_button row typed the required member `icon: string`, but §17.1 states fields named `icon` are identifiers and `contract.json` `field_types.icon` = `identifier` — two signals against the lone §17.4 inline type. This resolves OPPOSITE to amendment #64: the icon node's `name` is a string because `name` is not in §17.1's identifier list, whereas `icon_button.icon` is a field NAMED `icon`, so §17.1's identifier rule governs. The §17.4 row is corrected to `icon: identifier`, aligning the prose with §17.1 and the contract projection. No contract or golden change (golden 37 uses `icon:"menu"`, a valid identifier); the field-name convention now holds uniformly — `icon`-named fields are identifiers, `name`-named fields are strings. | none (prose; `contract.json` `field_types.icon` already `identifier`) |  |
| 66 | 2026-07-24 | §17.5, §17.1 (contract) | **`fill` is context-dependent — contract projection corrected.** `contract.json` `field_types.fill` was `boolean`, but `fill` means two different things: a layout boolean on `row`/`column`/`flow_row` (§17.1 "boolean-valued `fill`") and a **Color** on a canvas op (§17.5 "`CanvasOp.fill` is a Color"). The global by-name `field_types` map committed `fill` to `boolean` only, so the contract's sole structured description of canvas `fill` contradicted §17.5, while the CanvasOp Color type (which §17.1 says MUST be projected) was projected nowhere. `field_types.fill` is corrected to `varies-per-node`, matching the contract's existing escape hatch for the other context-dependent members `value` and `items`. No prose change and no golden change; `validate.py` does not gate `field_types` values (it stays green). | contract.json `field_types.fill` boolean -> varies-per-node |  |
| 67 | 2026-07-24 | §18.6 | **Reminder presentation gains its lower bound.** §18.6's only presentation obligation was "MUST present the reminder *at most once*" — an upper bound trivially met by zero presentations, so a Companion that never fires any reminder satisfied every MUST, and the "surviving registrations will never fire" consequence lived only in the non-normative note from amendment #43. This is amendment #46's genre ("at most one" permitting zero where exactly-one is intended) left unfixed for reminders. §18.6 now requires an accepted, unfired reminder to be presented *at least once* — promptly at or after `at_ms`, including promptly at restore or re-arm when `at_ms` passed while the process or device was down — and *at most once* per accepted `(owner, id, at_ms)` tuple, unless the tuple is removed or replaced before presentation or the platform notification permission is withdrawn (the obligation then resumes on the next opportunity after permission is restored if the tuple has not fired). Clarifying prose making the intended firing mandatory and convictable; no contract or golden change. | none (prose) |  |
| 68 | 2026-07-24 | §16.1 (cross-ref §16.3, §17.1, §12) | **An optional member present with an invalid value is a malformed node.** §16.1's whole-update rejection list named only "invalid *required* field"; §16.3 covers *unknown* fields (ignore) and §17's preamble covers *omitted* optionals (default), so a known optional member present with a value outside its declared type or domain — `alpha: 1.5`, `max_lines: 0`, `padding: -3`, a non-boolean `clip` — matched no rule, and §17 rows are inconsistent (some say "otherwise the node is invalid", `progress.value MUST be 0..1` states no consequence). §17.1's no-coercion rule forbids clamping but did not choose between ignore-member and reject-update. §16.1 now states that an optional member present with a value outside its declared type or domain makes the node malformed and rejects the whole update with `1201 content-invalid`, consistent with the no-coercion rule — except where a specific member defines a safe fallback for an unrecognized value under §12 rule 6 (`text.style` to `body`, `dialog.style` to `dialog`), which is applied instead of rejection. Clarifying prose closing an enumeration gap; no contract or golden change. | none (prose) |  |
| 69 | 2026-07-24 | §15.3 | **The delivery pump runs on an invariant, closing the SYNCING-backlog gap.** §15.3 started the pump only on "admission of a *new head event*" while `READY`, but an event admitted during `SYNCING` or into a non-empty queue is not a new head event, and §10.3 only says the Companion *MAY* release events after the flush while Emacs's `queue.replay` retry is gated on a nonzero `remaining` (reported `0`) — so a `queue`-policy tap taken during `SYNCING` with an exhausted barrier replay could sit undelivered until `ttl` expiry silently deleted it, data loss between two conformant implementations. §15.3 now states the pump MUST run whenever the session is `READY`, the queue is non-empty, no durable `event.action` request is in flight, and the pump is not paused by a prior transient error — regardless of whether the head was admitted while `READY` or during `SYNCING` — and entering `READY` after the §10.3 flush MUST start it. Clarifying prose replacing an under-inclusive trigger; no contract or golden change. | none (prose) |  |
| 70 | 2026-07-24 | §13.6 (cross-ref §13.2, §16.1, §17.4) | **A dirty draft is cleared only by the enumerated conditions, and local-editor text survives a refresh.** §13.6 forbade overwriting a dirty value "merely because the snapshot was refreshed", but a snapshot carrying a *changed* authored value is arguably not a mere refresh and the four-item clear-list was never stated exhaustive, so two conformant Companions could keep or overwrite a half-typed input on the same push — data loss. Separately, §13.6 scoped local-editor draft protection to `publish_state: true`, leaving a `publish_state: false` editor (user types, `on_save` captures) protected by no rule, with §13.2's atomic replace and §16.1's rendering-state retention unresolved. §13.6 now states the four clear conditions are the *only* circumstances in which a compatible dirty value is cleared — a changed authored value alone does not overwrite a draft — and that a local `editor` node's live text is preserved across a same-presentation-identity snapshot regardless of `publish_state`, the authored `value` seeding only a new presentation identity. Clarifying prose making the draft-wins-unless-reset design normative; no contract or golden change. | none (prose) |  |
| 71 | 2026-07-24 | §19.3 (cross-ref §19 preamble) | **Emacs must reconcile an `edit.open` seed that differs from its document.** §19 pins the Companion's seed text exactly (`edit.open.text` equals the selected volatile-or-cached seed) but defined no Emacs duty when that seed differs from Emacs's own document — and divergence is guaranteed: deltas are notifications, so a delta applied to the shadow can be lost at transport cut (seed newer than the document), and process death falls back to the cached authored `value` even when Emacs accepted later deltas (seed older). Emacs could then silently issue a whole-document `edit.apply` over the user's visible text, or adopt a stale seed discarding its own accepted edits — both silent data loss with no rule violated. §19.3 now requires Emacs, on receiving `edit.open`, to compare `edit.open.text` against its current document for that `document` and, where they diverge, to reconcile explicitly (adopt the seed, issue a reconciling `edit.apply`, or surface a conflict); Emacs MUST NOT silently overwrite a seed newer than the last delta it accepted, and MUST NOT issue a blind whole-document replacement that discards the user's visible text. Clarifying prose adding the missing receiver duty; no contract or golden change. | none (prose) |  |
| 72 | 2026-07-24 | §9.1 (cross-ref §21.1, §18.6) | **Revocation erasure is durable and resumable; "atomically" names the fence.** §9.1 required revocation to "atomically fence the identity … erase its token, queued payloads, … reminders, triggers, …", but "atomically" grammatically attaches to the fence while the erasure spans stores that cannot be one platform transaction, and nothing required the erasure to be durable or resumable. Because §21.1 and §18.6 make trigger firing and reminder presentation device-lifetime obligations driven by the persisted registration itself, a crash after fencing but before erasing reminders and triggers leaves a fenced identity whose registrations keep firing and whose keystore-encrypted SMS and call payloads (§21.5) survive on disk indefinitely, since the fenced identity is never revisited. §9.1 now requires revocation to be recorded durably before or atomically with the fence, and the erasure of every listed category to be completed — resumed after any process or device restart — until it is gone; a crash mid-erasure MUST NOT leave a fenced identity whose triggers or reminders still fire or whose sensitive queued payloads survive. Clarifying prose closing a crash-durability gap; no contract or golden change. | none (prose) |  |
| 73 | 2026-07-24 | §20.3 | **Absent intent-allowlist plural members deny by default; "when present" disambiguated.** §20.3 stated exact-match "including matching absence" only for `action`, `mode`, `package`, and `class_name`; for the plural members it left the semantics of an *absent* member undefined and "when present, a listed authority" ambiguous between "authority present in the URI" and "the `authorities` member present in the entry". An entry with `schemes` but no `authorities` matched a request whose data URI carried an authority under one reading (absent means unconstrained, launch) and failed it under the other (absent means empty allowlist, deny) — an allow-versus-deny divergence on the module's security boundary. §20.3 now states an absent `schemes`, `authorities`, `mime_types`, or `extra_keys` member is an empty allowlist, not an unimposed constraint: a request carrying a data URI, a URI authority component, a MIME type, or an extras key that the corresponding member does not list MUST NOT match, defaulting to deny, consistent with the positive-knowledge posture; and "a listed authority" is gated on the URI carrying an authority component. Clarifying prose pinning the security boundary; no contract or golden change. | none (prose) |  |
| 74 | 2026-07-24 | §23.3 (cross-ref §21.4) | **The MUST-NOT-log floor is aligned with §21.4's sensitive-source set.** §23.3's hard "MUST NOT appear in normal logs … or crash reports" list named "SMS bodies, call numbers" but not calendar event titles or times, nor the SMS *sender* identifier, though §21.4 groups `calendar.event` with `sms.received` and `call.state` as a sensitive source requiring source-to-sink approval and the §21.5 `calendar.event` row carries no redaction clause — so calendar titles and SMS sender numbers fell only to the next sentence's SHOULD, and a genuinely sensitive value could leave the device in a crash report from one conformant implementation and be redacted by another. §23.3 now adds calendar event titles and times, SMS sender identifiers, and the captured fire data of any privacy-sensitive trigger source named in §21.4 to the hard MUST-NOT enumeration, referencing §21.4 so the two stay in sync. Clarifying prose extending an existing floor; no contract or golden change. | none (prose) |  |
| 75 | 2026-07-24 | §21.3, §21.7, §20.1 (contract) | **`time.window` named as the predicate-only state type the contract already projects.** §21.3 required "every predicate type appears in `device.state_types`" for Emacs to include a gate, but §21.7 makes `time.window` a valid `when` predicate while excluding it from sampleable state types, and never said whether it appears in `device.state_types` — so a literal reading made every time-window-gated trigger unauthorable (Emacs MUST omit it), while `contract.json` already carries `predicate_only_state_types` holding `time.window` and omits it from `state_types`, a projection no prose defined. §21.7 now names `time.window` the sole predicate-only state type — a valid gate predicate that never appears in `device.state_types`, `device.trackable_state_types`, `state.get.types`, or a `state.edge` — and §21.3 excepts predicate-only types from its `device.state_types` requirement (a predicate-only type is available in a gate whenever `triggers` or `state.get` is granted). §20.1 notes `device.state_types` excludes predicate-only types. Clarifying prose aligning the spec with the existing `predicate_only_state_types` projection; no contract or golden change. | none (prose; contract already correct) |  |
| 76 | 2026-07-24 | §21.1 (cross-ref §21.4, §21.8) | **Permission grant state is not a trigger-acceptance gate, and "required permission descriptor" is defined.** §21.1 required validating "every … required permission descriptor" before changing registrations but never defined the phrase, inviting a reading where a currently-withdrawn permission rejects the whole atomic replace-set — yet §21.8 says blocked registrations "MAY remain stored and MAY arm after permission is restored" and §21.4 says "an unavailable permission MAY leave the trigger registered but unarmed", so `triggers.set [battery-low, sms-watch]` with SMS off yielded total success on one Companion (store sms-watch dormant, `{count:2}`) and total failure on another (`1101`, and because rejection is atomic, battery-low uninstalled too). §21.1 now defines a "required permission descriptor" as the OS permission(s) a trigger's `type` (per the §21.5 catalog) and its `on_fire` capabilities need, and states that a shape-valid entry whose `type` appears in `device.trigger_types` MUST be accepted and stored even when a needed permission is currently withdrawn — stored unarmed per §21.8 and armed when permission is restored; permission grant state MUST NOT be a `1101` rejection cause and MUST NOT fail the atomic replace-set. Clarifying prose resolving an accept-versus-reject divergence; no contract or golden change. | none (prose) |  |
| 77 | 2026-07-24 | §5.2 (cross-ref §22.3) | **Accepting a new loopback connection is required, so supersession is reachable.** §5.2's supersession MUST fired only "when a new session authenticates successfully", but no rule obliged the Companion to *accept* a second TCP connection while one is open, and §22.3 bounds queues and requests but never connection count — so a single-connection Companion that refuses a second dial while a session is open satisfied the MUST vacuously, and when Emacs is OS-frozen with a `READY` connection still open (no FIN), the user's restarted Emacs could never reconnect until the Companion itself restarted: total connectivity loss between two conformant implementations. §5.2 now states the Companion MUST continue to accept new loopback connections and allow each to attempt the handshake while an authenticated session exists — supersession depends on it — and MUST bound the number of concurrent pre-authentication connections; a listener that refuses a second dial while a possibly-zombie session is open is non-conformant. Clarifying prose making the acceptance precondition explicit; no contract or golden change. | none (prose) |  |
| 78 | 2026-07-24 | §9.1 (cross-ref §23.3, §21.5) | **The pairing token is protected at rest at least as strongly as the data it authorizes.** §9.1 required only "storage private to each endpoint" for the 128-bit pairing token, while §23.3 and amendment #48 mandate keystore-backed encryption for sensitive queued trigger data — so the master secret gating all authentication, capability invocation, and trigger arming had weaker at-rest protection than a single SMS body, and a forensic or rooted image (post-first-unlock file-based encryption) could yield the plaintext token and complete the §9.3 handshake as a fully authorized Emacs. §9.1 now requires the pairing token and any recoverable HMAC key material to use platform keystore-backed storage wherever one exists, on the same unconditional terms as §21.5's sensitive queued data, and never to be held in plaintext application storage weaker than §23.3's protection for the SMS and call data the token's authority can reach; a Companion on a platform with no keystore facility MUST make that limitation explicit and MUST NOT persist a pairing under weaker protection. Clarifying prose raising the at-rest floor; no contract or golden change. | none (prose) |  |
| 79 | 2026-07-24 | §9.2 (cross-ref §9.1, §9.3) | **A hello with an unknown pairing ID is challenged and rejected with equal work.** §9.1 required not revealing whether an unknown pairing ID or an incorrect proof caused authentication failure, but §9.2 defined hello outcomes only for a protocol mismatch (`1202`) and an invalid field (`-32602`), and §9.3's constant-time rule covered only comparison of "well-formed proofs" — with no stored token there is no expected proof, so nothing required challenge-anyway or equal-work verification on the unknown-ID path, letting an implementation leak the cause through the failure stage (`1203` at hello versus at `auth.response`) or through response timing (a skipped HMAC) while satisfying the letter. §9.2 now requires a well-formed `session.hello` whose `pairing_id` matches no stored pairing to receive a normal fresh `server_nonce` challenge, indistinguishable from a known ID, and the subsequent `auth.response` to be verified with work equivalent to the known-ID path — an HMAC against a fixed dummy key plus a constant-time comparison — and rejected with `1203`, so neither the failure stage, the error code, nor response timing distinguishes an unknown pairing ID from an incorrect proof. Clarifying prose supplying the mechanism §9.1's non-revelation MUST needs; no contract or golden change. | none (prose) |  |
| 80 | 2026-07-24 | §4.2, §7.5 (cross-ref §7.2, §4.3) | **Amendment #34 residue swept from §4.2 and §7.5 (editorial).** #34 rewrote §7.2 to allow a JSON string or a safe integer request ID so core `jsonrpc.el` conforms, but two contradicting sentences survived: §4.2's "EBP request IDs are strings; Section 7.2 prohibits numeric request IDs" (a now-false cross-reference) and §7.5's `rpc.cancel` "the exact *string* request ID". The operative rules (§7.2 acceptance, §7.5 "exact ID" plus §4.3 equality) already determine correct behavior — a Companion rejecting an integer ID is non-conformant, and integer-ID cancellation works via §4.3 equality — so this is editorial staleness, not an interop hole (the Opus verification downgraded the earlier P1 framing). §4.2 now reads "strings or safe integers as Section 7.2 defines"; §7.5 now cancels "the exact request ID (a JSON string or a safe integer), matched by Section 4.3 equality". Not wire-breaking. | none (editorial prose) |  |
| 81 | 2026-07-24 | §15.2 (cross-ref §14.4, §15.1) | **`occurred_at_ms` is stamped from the effective wall clock.** §15.2 compares the rollback-protected effective wall clock against `occurred_at_ms`, but §14.4 defined `occurred_at_ms` only as "Creation time" and §4.2 only as epoch milliseconds, so the stamping clock was unpinned — a raw device-clock stamp taken after a backward clock change larger than `ttl_s` yields `occurred_at_ms` below high-water minus ttl, satisfying the expiry predicate at admission, so the event is deleted before any delivery and the user's intent is silently lost. §15.2 now requires a durable event's `occurred_at_ms` and `queued_at_ms` to be read from the effective wall clock (equivalently, the stored expiration MUST grant the full `ttl_s` of effective-clock lifetime from admission). Mirrors amendment #47's pinning of `time.at_ms`. Clarifying prose; no contract or golden change. | none (prose) |  |
| 82 | 2026-07-24 | §17.1, §17.5 (contract) | **`selected` is context-dependent — the by-name projection is corrected.** Amendment #66 applied to a second field it missed: §17.1's by-name list made `selected` a plain boolean and `contract.json` `field_types.selected` was `"boolean"`, but §17.5 states "`selected` is one `YYYY-MM-DD` date" for `month_grid`, so a validator keyed on the global map rejects a valid `month_grid` value as a type error and per §16.1 rejects the entire surface with `1201`. §17.1 now qualifies the field as "boolean-valued `selected`" (exactly as #66 qualified `fill`), and `field_types.selected` becomes `"varies-per-node"` (boolean on `chip`, `YYYY-MM-DD` string on `month_grid`). Additive and corrective; no member or enum changes. | contract `field_types.selected` boolean -> varies-per-node; validate.py green |  |
| 83 | 2026-07-24 | §10.3 (cross-ref §14.5, §14.6) | **The on-`READY` flush stamps the revision §14.6 requires, not the current one.** §10.3's flush emitted `state.changed` "using the accepted revision currently shown", but §14.6 requires `revision_seen` to equal the revision presented when the edit was made and forbids rewriting it after a newer snapshot — so for a value edited at revision 41 while `SYNCING` pushed 42, the two rules stamped different revisions and Emacs's draft-adoption test kept or dropped the offline draft depending on the Companion; §10.3 also omitted §14.6's `reset_input_ids` discard. §10.3 now requires each flushed notification to carry the §14.6 `revision_seen` (presented-when-edited, or the welcome revision for a disconnected edit), never the current revision, and to discard unsent any value whose ID an accepted higher-revision snapshot named in `reset_input_ids`; and records that this flush is the sole path by which a `SYNCING`-time edit reaches Emacs. Editorial contradiction fix aligning §10.3 with §14.6 (Opus downgraded the earlier P1 framing to P3); no contract or golden change. | none (prose) |  |
| 84 | 2026-07-24 | §4.5, §19 preamble, §19.4 (cross-ref §23.5) | **A numeric bound on the synchronized editor document.** `edit.open.text` and the `edit.resync` result MUST carry the complete document in one frame, but §4.5 bounded `max_frame_bytes` and `max_editor_sessions` with no limit on document length, and deltas grow a document within per-frame caps — so two conformant endpoints can reach a document whose mandatory full-state carrier cannot be framed, forcing a §6 close and a permanent connect/close loop on reconnect (the node is present in `READY`, so `edit.open` MUST carry an unsendable seed). A new `max_editor_bytes` limit (REQUIRED when `editor.sync` is granted, at least `65536` and no greater than `max_frame_bytes` minus 4096) closes it: Emacs MUST NOT present an editor whose document text exceeds it, and a text-changing `edit.apply` that would exceed it MUST receive `1201 content-invalid` with `data.reason: "editor-too-large"`. Mirrors amendment #40's `max_device_report_bytes`. Additive: an over-limit document was previously unframeable, so no conformant editor is invalidated. | contract `limits.welcome` += `max_editor_bytes`; validate.py green |  |
| 85 | 2026-07-24 | §14.1 (cross-ref §5.3, §15.1) | **A `wake` descriptor without the `offline.wake` grant is content-invalid, and the wake signal is gated on the grant.** §14.1 banned Emacs from authoring `wake` without the grant (sender-only), but stated no RECEIVER duty for a `wake` descriptor arriving in a session that never granted `offline.wake`, and §5.3 gates the wake signal on a persistently configured target — so a Companion with a target configured in a prior session could durably admit and OS-signal a wake the current session never negotiated (Opus verification confirmed this as a P1 security divergence). §14.1 now states the wake signal is authorized by the current session's `offline.wake` grant, not by target configuration alone; a descriptor whose `when_offline` is `wake` in a session without the grant MUST be rejected with `1201 content-invalid`, and the Companion MUST NOT signal any wake target for it. Clarifying prose adding the missing receiver rule; no contract or golden change. | none (prose) |  |
| 86 | 2026-07-24 | §17.2, §9.1 (cross-ref §13.5) | **Fetched image content: cache license, offline use, and revocation erasure defined.** §17.2 fully specified the image fetch (redirect cap, timeout, SSRF checks) but said nothing about the fetched or decoded bytes afterward — whether they may be cached, whether a cached image satisfies §13.5 disconnected rendering, and whether an image cache is inside §9.1's revocation-erasure boundary (whose list does not naturally include a URL-keyed image disk cache) — so a Companion could retain personal photos after revocation while another wipes them, defeating §9.1's guarantee (Opus verification confirmed this as a P1 privacy divergence, the ratified boundary genre of #48/#49/#72/#78). §17.2 now states fetched or decoded image content MAY be cached but the cache MUST be app-private, scoped to the pairing identity, and erased on revocation, and MAY satisfy §13.5 offline rendering (placeholder otherwise); §9.1's erasure list adds cached fetched image content. Clarifying prose; no contract or golden change. | none (prose) |  |
| 87 | 2026-07-24 | §13.4, §13.7 (cross-ref §13.1, §10.2, §22.1) | **The `tile:*` SurfaceSpec variant, defined.** Amendment #39 registered `tile:<name>` as a capability-gated namespace (§13.1), added `tile` to §10.2's applicable targets, and registered `surfaces.tile` (§22.1) — calling it "exactly parallel to notification/widget" and enumerating the §13 rules it inherits as "revision/tombstone/limit/update/remove". It never touched §13.4, whose sentence "The namespace determines the exact SurfaceSpec variant" makes that table the sole authority and whose closing sentence makes any unlisted combination `1201 content-invalid`. A granted `surfaces.tile` was therefore unusable: Emacs had no shape to author and a Companion none to validate, so identical bytes are accepted by a Companion reading "unlisted means app-like" and rejected by one reading "parallel to widget". §13.4 gains a `tile:*` row defining the variant as a flat Quick-Settings metadata object `{label, icon?, subtitle?, active?, on_tap?}` — a fixed host slot, so no Node body and no stateful node — and its closing sentence now names every namespace that MUST NOT carry `views`. `active` reports the slot's on/off state; `on_tap` enters the §14 pipeline with no surface/dialog/revision context and the Companion injects the tile `<name>` as `tile`, parallel to §20.3 shortcuts. §13.7 records that a tile carries neither `header_action` nor `empty`. Additive: no tile `spec` was previously definable, so no previously valid frame is invalidated. | contract `surface_spec_variants.tile` (first projected variant shape); validate.py green |  |
| 88 | 2026-07-24 | §22.4 (new), §10.2, §24.2 | **The feature registry.** §10.2 requires each profile to carry a `features` array and requires Emacs to "gate every emitted node, builtin, and constraining feature against the target profile", and §24.2 lists per-target feature gating as an Emacs core-conformance duty — but the document never enumerated the feature namespace. Normative feature names appear in exactly two places (`image.https`/`image.data` in §17.2, `toolbar.<identifier>` in §17.7); §22.1 registers protocol capabilities, §20.3 device capabilities, §21.5 trigger types, and `contract.json` projects capabilities, trigger/state types, theme and syntax roles — but nothing projected features. A sender therefore could not write the gate §24.2 requires: it could only hard-code today's two families, leaving any later or vendor feature ungatable by construction and leaving two senders free to disagree about which constructs are "constraining". New §22.4 registers the vocabulary the spec already relies on, giving each entry its constrained construct and the sender's omit-rule when absent; §10.2 and §24.2 cross-reference it and it projects as `features`. Appended as §22.4 rather than inserted, so no existing §22.2/§22.3 cross-reference renumbers. This is amendment #57's genre (enumerate the vocabulary the spec already assumes). Additive: the two existing feature names and their §17.2/§17.7 rules are unchanged. | contract `features`; validate.py green |  |
| 89 | 2026-07-24 | §17.6 | **Snackbar presentation lifetime and re-show semantics.** §17.6 defined the member as `snackbar: string` and pinned exactly one behavior — "The Companion MUST dispatch a snackbar action only on a user tap, never on timeout" — saying nothing about when a snackbar is presented, for how long, or whether a later accepted snapshot carrying the same string presents it again. Because a snapshot is a complete replacement (§13.2), a Companion treating `snackbar` as snapshot state dismisses it as soon as the next snapshot omits the member, so a message can vanish before it is read; one treating every accepted snapshot with a non-empty `snackbar` as a fresh presentation re-flashes a builder-constant string on every push. Both readings were conformant and the divergence is reachable within a second of any debounced re-push. §17.6 now keys presentation to CHANGE under §4.3 equality against the previously accepted snapshot for that surface, forbids re-presenting an unchanged value, makes an absent or empty value dismiss a visible snackbar, asks for a 4-second minimum and user dismissal, and states that `snackbar` carries no §16.1 presentation identity. This is the #59/#60 genre. Clarifying prose; no contract or golden change. | none (prose) |  |
| 90 | 2026-07-24 | §21.3, §21.1 | **A trigger omitted for an unadvertised gate type, and the set it came from.** §21.3 said "If a non-predicate-only type is absent, Emacs MUST omit the entire trigger; it MUST NOT remove the unsupported predicate and install a weaker trigger" — a per-trigger rule. But `triggers.set` (§21.1) atomically replaces the WHOLE set for the pairing identity, and the SPEC never said what becomes of the other entries, nor whether the omission must be visible to the application: one sender silently drops the offending trigger and installs the rest, so the app believes it registered N registrations while N-1 can fire and only the returned `count` betrays it; another refuses to send at all, so one unauthorable trigger costs every other registration. Both satisfied §21.3's letter. §21.3 now states the omission is per trigger — the remaining entries of the same `triggers.set` are still sent and `count` reflects only those — and that Emacs MUST surface an omitted trigger to the application rather than drop it silently, since `count` is the sole wire evidence and no rule requires a caller to read it. §21.1 cross-references. Clarifying prose resolving a real sender divergence; no contract or golden change. | none (prose) |  |
| 91 | 2026-07-25 | §6.2 (cross-ref §22.3, §23) | **The delegation clause is bounded: sender duties, termination, diagnostics, pre-validation resources.** §6.2 relaxed receiver strictness for the Emacs endpoint on the grounds it MAY delegate framing and decoding to a host JSON-RPC library, "accepting that library's tolerances", disclaiming relaxation only for Section 6.1's sender duties. Three axes were left open, and a source-grounded review of the blessed delegate (core `jsonrpc.el` on Emacs 30.1) found a live divergence on each: the clause was silent about the delegate's DIAGNOSTICS, so the library's default full-frame event log records authentication proofs and submitted passwords against §23.3's MUST NOT; silent about resources the delegate commits BEFORE validation, so wire-supplied member and method names intern into a process-lifetime table no §23.5 bound names; and "tolerances" cannot cover NON-TERMINATION — a tolerance is "I accepted what I could have rejected", while a permanent silent header stall is no reaction at all. The old disclaimer also named only Section 6.1, leaving §7.1's `params: {}`/`result: {}` and §4.1's absent-vs-`null` sender rules formally on the delegable side. The paragraph now disclaims relaxation of every sender obligation in this document; requires an excused role to reach a bounded terminal reaction — close, discard, or a documented tolerance within §22.3's bounds, never silent acceptance as valid, never an unbounded stall; and states the delegation does not extend to a host library's diagnostics or to resources it allocates before validation, nor relax any Section 23 obligation. Keystone of the 2026-07-25 Emacs-30.1 review batch: the follow-on amendments are local elaborations of this rule. Clarifying prose; no contract or golden change. | none (prose) |  |
| 92 | 2026-07-25 | §23.3, §8 (cross-ref), §24.6 item 12 | **A delegated host library's diagnostics are inside §23.3's exclusion.** §23.3 forbade authentication proofs, passwords, and private editor content in logs and diagnostics, and §6.2 (as bounded by #91) still allowed delegating framing/decoding to a host JSON-RPC library — but nothing said the delegate's own diagnostics inherit the exclusion, and the blessed delegate violates it by construction: core `jsonrpc.el` 1.0.25's events buffer defaults to `(:size nil :format full)` with `jsonrpc--log-event` on `jsonrpc-event-hook`, recording every frame body verbatim in both directions with the trim branch never taken, plus `display-warning` for undecodable bodies — reproduced live: the auth `client_proof` and a submitted password both landed in a persistent buffer, making §24.6 item 12 unpassable on the Emacs endpoint. §23.3 now requires an endpoint that delegates framing, decoding, or dispatch to disable or redact every delegate facility recording raw frames or payloads BEFORE the first frame, never to assume the facility is off by default, and to bound any developer-enabled retention in size and lifetime; §8 cross-references; §24.6 item 12 widens to authentication proofs and host-library message traces. Emacs-30.1 review P1-1. Clarifying prose; no contract or golden change. | none (prose) |  |
| 93 | 2026-07-25 | §23.5, §4.5, §11, §7.3, §24.6 item 4 | **Wire-supplied names must not grow a process-lifetime interning table, and method names get the bound and grammar every other wire string already had.** §23.5's bounded-resource list was entirely per-object; nothing bounded the union of distinct NAMES across frames, and on the blessed Emacs path the allocation is permanent and pre-validation: `json-parse-string` interns member names for plist/alist object types (not hash-table), `jsonrpc.el` hard-codes plist and `intern`s the method name inside the dispatcher call — measured on 30.1: +20,000 obarray symbols from 20,000 distinct plist keys surviving gc, RSS 56→221 MB from four 3.28 MB bodies, while hash-table decoding plateaus; §12 rule 1 and §7.3 REQUIRE tolerating the unknown names that feed it, and §23.6 concedes loopback reachability pre-auth. Separately §4.5 bounded identifiers (128) and request IDs (64) but placed no bound and no grammar on `method`. §23.5 now forbids letting a peer's names grow an unbounded process-lifetime pool (select a non-interning mode or bound and reclaim; rejecting after decoding does not satisfy — the allocation already happened; not relaxed by §6.2 since the cost is cross-session); §4.5 adds the Method-name row (128 octets, §4.4 grammar); §11 rejects a non-identifier method with `-32601`/log-and-ignore without consulting the registry; §7.3 forbids allocating non-reclaimable state keyed by an unvalidated method name; §24.6 item 4 gains the over-long/non-identifier method vectors and the distinct-names growth check. Emacs-30.1 review P1-2. | contract `limits.fixed.max_method_bytes: 128`; validate.py green |  |
| 94 | 2026-07-25 | §5.3, §21.1 (cross-ref) | **The wake signal MUST be inert.** §5.3 constrained what the wake signal must not CONTAIN (token, payload, proof) and forbade the promiscuous target shapes, but never constrained what the signal can CAUSE — and on the primary target platform the only reachable component of the Emacs host is its launcher activity, which accepts a startup-argument array spliced directly into the command line with no caller check: a §5.3-conformant Companion could attach `--eval` or `-l /sdcard/x.el` to the wake intent — arbitrary code execution in the endpoint holding the pairing token, before any authentication, in exactly the cold-start case wake exists for. The amendment does not fix the host platform; it stops a conforming Companion being the vehicle: the signal's payload is limited to the non-secret pairing ID and MUST NOT carry or be interpretable as startup arguments, a command line, code to evaluate, a file/library to load, an init file, or an environment override; a target that requires such a parameter MUST be rejected; inertness is re-verified immediately before each send alongside the existing target re-check. §21.1's `policy: wake` cross-references §5.3 in full. Emacs-30.1 review P1-3. Restricting prose (a previously conforming signal carrying extras is now non-conforming); no contract or golden change. | none (prose) |  |
| 95 | 2026-07-25 | §9.1 | **The token-storage rule gets a subject and a floor.** §9.1's "Where a platform provides keystore-backed encrypted storage, the pairing token ... MUST use it" bound BOTH endpoints with no subject restriction, while the only relief sentence named "A Companion on a platform with no such facility" — and the Emacs endpoint is non-conformant by construction on the primary platform: Android provides the facility but Emacs 30.1 exposes no keystore interface from Lisp (the complete Android DEFUN surface is frames/colors/battery/clipboard/notifications/storage-access; the only `keystore` hits in the tree are the APK signing key), so amendment #78's rule was unsatisfiable for one of the two parties it bound. The keystore MUST is now explicitly the Companion's, unconditional as before; an endpoint whose platform HAS the facility but exposes no interface from its implementation environment — and an endpoint on a platform without one — meets a defined floor instead: app-private storage under the endpoint's own identity in the most restrictive mode offered, never shared/browsable/exported locations, with the reduced protection made explicit at pairing time; an endpoint that cannot meet the floor MUST NOT persist the pairing and re-pairs each session. Informative note flags document-provider exports and shared-user-id installs as floor-breakers. Emacs-30.1 review P1-4; revises #78 (whose rationale was Companion-side). Clarifying prose; no contract or golden change. | none (prose) |  |
| 96 | 2026-07-25 | §19 preamble | **The synchronized document must be Unicode scalar values, losslessly.** §19's only presentation precondition was the `max_editor_bytes` size test; nothing made a document whose text is not a sequence of scalar values ineligible, and nothing forbade substituting a representable character to force eligibility. The live failure is silent: host text models admit positions that are not scalar values (Emacs retains undecodable raw bytes verbatim as char codes above `#x3FFF80`), §4.1 already bars emitting them, and the natural defensive fix — a 1:1 U+FFFD substitution — PRESERVES scalar length, so every §19.3 length gate keeps passing while shadow and buffer silently differ, and the first Companion splice covering that position writes U+FFFD into the user's file; one stray byte in an org file is enough. Emacs MUST NOT present a synchronized editor whose text is not scalar values; no endpoint may substitute, drop, or replace characters to make a document, splice, delta, or resync result representable — an unrepresentable document is ineligible; a document that acquires such text mid-session is withdrawn via the existing §19 mechanisms (node removal, tombstoning, document change, presentation-identity change), never lossily spliced. frames.golden gains an astral `edit.delta` row (row 36) so a UTF-16-counting Companion fails a golden. Emacs-30.1 review P1-5. | goldens/frames.golden +row 36 (astral edit.delta); validate.py green (37 frames) |  |
| 97 | 2026-07-25 | §24.2 | **The Emacs version floor, stated where conformance lives.** The document never named an Emacs version, yet two normative clauses are only true from a floor: §4.5's amendment-#35 recursion carve-out assumes a host JSON-RPC library whose native JSON decoder bounds recursion by its own means — `json.o` is unconditional in Emacs 30.1's src/Makefile.in but configure-gated in 29.x, where the blessed `jsonrpc.el` silently degrades to json.el's RECURSIVE reader and a depth-64 body becomes a stack risk the carve-out claims impossible — and §9's HMAC-SHA-256 relies on `secure-hash`, unconditional core in 30.1 (GnuTLS-backed `gnutls-hash-mac` is an optional add-on on Android). The floor lives in §24.2 — already the Emacs-specific conformance section — so §1/§4 stay implementation-agnostic: an Emacs endpoint claims conformance for GNU Emacs 30.1 or later, and a port to an older host MUST re-derive both calibrated properties rather than assume them. Motivated by the 2026-07-25 review's method: the deployment every requirement is judged against is Emacs 30.1 built for and running ON Android. Clarifying prose; no contract or golden change. | none (prose) |  |
| 98 | 2026-07-25 | §19.4 | **The move-only `edit.apply` carries `seq`, and a stale one is refused.** §19.4 described the move-only form as omitting "all of `start`, `del`, `text`, and `len`" and "retain[ing] the current seq", then said only "A move-only form succeeds only at the current sequence" — never stating that the form CARRIES `seq` (contract.json has always made it REQUIRED for the method), nor what a receiver does when it does not match. Two conformant readings existed: omit `seq` entirely, which makes the succeeds-only sentence vacuous, or carry and compare it. Under the first reading a caret computed against a document state the Companion has already left is answered `applied`, so Emacs records a position the document no longer has — the same class of stale-intent acceptance §23.4 warns about, reached without any text change. §19.4 now states the move-only form carries `seq`, succeeds only when it equals the current sequence, MUST otherwise receive `{status:"stale", seq:<current>}` without moving caret or selection, and never advances the sequence. Found auditing the T1/T2 implementation: the Companion read `seq` on the text path and ignored it on the move path (fixed in the same commit). Clarifying prose pinning a real sender/receiver divergence; no contract change (the `seq` requirement was already projected). | none (prose) |  |
| 99 | 2026-07-25 | §4.2 (cross-ref §6.2, §4.5) | **A receiver's duty for a number the data model cannot carry, and its error code.** §4.2 stated the safe-integer range and the finiteness rule as properties of a value plus sender MUST NOTs ("A sender MUST NOT emit a literal that overflows to infinity"; "`NaN`, positive infinity, and negative infinity ... MUST NOT be transmitted"), but named no RECEIVER duty and no error code, so a receiver that accepts `9007199254740992` and one that rejects it were both defensible — and a receiver accepting it must either widen it to an implementation-specific type (which §4.2's own precision sentence forbids in spirit) or silently lose the value, while its peer, whose numbers are binary64, cannot represent it at all. §6.2's enumeration covers invalid UTF-8/JSON and non-object top levels; neither names this case. §4.2 now requires rejection as a **Parse Error**, on the stated precedent of §4.5's nesting-depth limit — whose golden (`21-deep-nesting.bin`) already pins a well-formed-JSON §4-layer violation to `parse-error` — because the failure belongs to decoding into this data model and precedes dispatch; the receiver MAY continue the connection per §6.2. Widening into an extended numeric type is forbidden. The asymmetry is made explicit and deliberate: a literal that UNDERFLOWS to zero converts to zero and is accepted, because zero is a representable EBP value whereas an infinity is not one at all. New negative golden. | `goldens/wire/22-integer-out-of-range.bin` + manifest row; validate.py gains the reference numeric check; validate.py green (17 fixtures) |  |
| 100 | 2026-07-25 | §19.3 (cross-ref §19.4) | **A local edit MUST be derived from the current shadow.** §19.4 gates an inbound `edit.apply` on "the current editor text still equals its synchronized shadow", but §19.3 stated no counterpart for the Companion's OWN local edits — it says "Local editing is applied to the shadow immediately", language that presumes the editing surface and the shadow are one object. That holds for a Companion whose text widget IS the shadow and fails for every Companion whose editing surface keeps its own copy — which is every Compose, UIKit, or web renderer: an inbound `edit.apply` moves the shadow while the surface still displays the previous text, and the next keystroke produces a splice expressed in the SUPERSEDED document's coordinates. Nothing in §19 forbade applying it, and the length equation cannot catch it: the Companion derives `len` from its own shadow, so the equation is satisfied by construction and only the bounds check stands between a racing keystroke and silent corruption of the user's document. §19.3 now requires a local edit to be derived from the shadow at the moment of application; a Companion whose editing surface holds its own copy MUST discard an edit whose pre-edit state is not the current shadow, without changing text or advancing `seq`, and MUST re-present the shadow. Names itself the local counterpart of §19.4's gate and records why the length equation is not a substitute. Found auditing T2 against §19. Clarifying prose closing a silent-data-loss hole; no contract or golden change. | none (prose) |  |
| 101 | 2026-07-25 | §6.2 | **The header line grammar, defined for receivers.** §6.2 closes the connection on "a malformed header line" and permits ignoring "an unknown syntactically valid ASCII header field", but never said what makes a line valid — so three conformant implementations of this very document disagreed on identical bytes. Differential testing of the two wire twins plus the reference validator found: a header name containing a space, a `0x01`, a `0x7f`, or a bare LF is closed by one, accepted by another, and accepted-with-a-different-outcome by the third; and because the header section is split on CRLF only, a bare LF inside a `Content-Length` VALUE let a line-anchored value regex match the digits before it (a real bug found in `ebp.el`, whose `^`/`$` match at line boundaries — `Content-Length: 2\nX: 1` was accepted as 2 while both other implementations closed). §6.2 now defines a header line as `name ":" [OWS] value` with `name` one or more octets in `0x21`-`0x7E` excluding `:` and `value` printable ASCII plus HTAB, names an empty/colon-less/control-carrying line as malformed, and requires `Content-Length` to be matched against the complete field value rather than any line within it. Restricting for a receiver that previously accepted these lines; no sender is affected (§6.1 already permitted only the exact `Content-Length` line). | none (prose); `ebp.el` and `validate.py` corrected to match |  |
| 102 | 2026-07-25 | §6.2 (cross-ref §4, §22.3) | **Multi-fault bodies: any one fault MAY be reported, and a pipelined message MUST NOT be stranded.** §6.2 maps single faults to codes but says nothing about a body violating two rules at once, so the three implementations answered the same bytes with different codes purely from detection order — a one-pass parser reports in document order while a phased validator reports by phase (`[9007199254740992]` is a Parse Error to one and an Invalid Request to another; `{"a":1,"a":2,` is the reverse). Nothing was wrong with either, but the divergence is indistinguishable from a bug and is exactly the cross-artifact drift genre. Separately — and more seriously — §6.2 permits continuing after a recoverable body fault without saying what happens to messages that shared the read: an implementation whose decode loop unwinds on the fault leaves a request PIPELINED BEHIND the bad frame buffered and unanswered, and if the peer is waiting on that response before sending anything further the session deadlocks, which amendment #91 and §22.3 forbid as an unbounded stall (found in the Companion, fixed in the same commit). §6.2 now states that any one applicable fault MAY be reported with no detection order imposed, that exactly one error response is sent with the code this document assigns to the fault reported, and that a receiver continuing the connection MUST deliver messages complete before the fault and MUST resume decoding those after it, because the stream stays synchronized across a body fault. Clarifying prose; no contract or golden change. | none (prose) |  |
| 103 | 2026-07-25 | §19.4 | **`max_editor_bytes` binds the seed as a receiver duty, not only the splice.** Amendment #84 bound the document three ways — Emacs MUST NOT present an over-limit editor, an inbound `edit.apply` past the limit gets `1201 editor-too-large`, and a Companion-local edit past it is refused — but every enforceable clause was about an EDIT. The seed had only the sender-side "MUST NOT present", with no receiver duty, so a Companion accepting a surface whose `editor` node carries an over-limit authored `value` reached a terminal state the spec never contemplates: the node is accepted, `edit.open` carries the whole over-limit document, and from that moment every edit in BOTH directions is refused by #84's own rules — a synchronized editor that is silently and permanently read-only, with no diagnostic to the user and none on the wire. §19.4 now requires the Companion to reject a surface or dialog mutation presenting a synchronized `editor` whose authored `value` exceeds `max_editor_bytes` with `1201 content-invalid` / `editor-too-large` before the node is accepted, giving the sender rule its enforcement point at the seed. Found auditing T2 against §19. Restricting: a previously accepted (and unusable) seed is now refused at acceptance, which is the outcome #84 intended. | none (prose) |  |
| 104 | 2026-07-25 | §19.4, §19 preamble | **One session per `(document, presentation identity)`, and presence at `READY` is evaluated against accepted state.** Two holes in the same seam, both reachable without a malformed frame. (a) §19 preserves a session across surface replacements by presentation identity and document, but never said the pair is EXCLUSIVE — so a second surface presenting the same pair was conformant, and a Companion keying sessions by that pair (the natural implementation) silently overwrote the first surface's session with no `edit.close`: Emacs then received `editor-stale` for a session it was never told had died, §19.4 forbids `edit.resync` from recreating it, and removing EITHER surface closed the survivor. §19.4 now makes the pair exclusive across accepted surfaces and outstanding dialogs, with `1201 content-invalid` / `editor-duplicate`. (b) §19's opening rule said a session is created when an editor "first becomes present in `READY`", which an implementation naturally reads as "when an update makes it present" — leaving two live gaps: a reconnection over cached snapshots receives no `surface.update` (legal per §13.5, the snapshot is unchanged and still rendered), so NO session was ever opened and every rendered editor was permanently read-only with no diagnostic; and an editor accepted during `SYNCING` whose surface was then removed still opened on `READY`, creating a session no close path could reach. §19 now states presence is evaluated against the accepted surface and dialog documents at the moment `READY` is reached, not against the mutations the connection observed, and names both consequences. Found auditing T2. Clarifying prose + one new receiver rejection; no contract or golden change. | none (prose) |  |
| 105 | 2026-07-25 | §19.4 (dialog scope, cross-ref §18.1) | **A dialog's synchronized editors are counted, opened, and closed with the dialog.** §19 already scoped the editor-session limit to "accepted surface **or dialog** documents" and §4.5 counts identities across both, but every other §19 rule is written in terms of surfaces, so a dialog's `editor` node sat in a gap: an implementation could render it as synchronized (its `dialog` profile advertises `editor`) while never opening a session, never counting it toward `max_editor_sessions`, and never closing anything when the dialog completed — the node presents as synchronized and is never synchronized, and `editor-session-limit` can never fire from a dialog at all. This amendment does not add a rule so much as make the existing one enforceable: it is recorded here because the audit found all three behaviors missing in an implementation that passed every other §19 test, which means the text was not carrying the requirement. The Companion counts a dialog's identities with the surfaces', opens sessions when the dialog is presented, and closes them when the dialog is submitted, dismissed, cancelled, or lost with the connection. Editorial scope clarification; no contract or golden change. | none (prose) |  |
| 106 | 2026-07-25 | §22.3 (cross-ref §15.3) | **A sender's own outstanding-request ceiling is not an inbound-overload close, and a local refusal is not a peer answer.** §22.3 requires every endpoint to bound its outstanding request count, then describes exactly one exhaustion protocol — `log.error` `1401` followed by "It MUST then close the connection" — whose message reads "**Inbound** processing capacity exhausted". So the rule that a sender must bound its own outbound requests had no stated behavior for reaching that bound, and the only nearby protocol says CLOSE, which is wrong for a self-inflicted condition that clears as answers arrive. Implementing the bound exposed the second, sharper half: the refusal has to conclude the request somehow, and concluding it with an error makes it indistinguishable from a peer's error response — in the Companion this fed §15.3's replay pump, which reads any error as a peer answer, so the pump paused itself and published `blocked_by: "overloaded"` for an error the peer never sent, stalling durable delivery on our own load. §22.3 now states that reaching the sender-side ceiling MUST NOT close the connection for that reason alone; the excess request is failed locally, concluded exactly once with `1401 overloaded`, never placed on the wire, and MUST NOT be recorded as a received error under §15.3; and a delivery mechanism that is already single-flight (§15.3's pump) is not bounded by the ceiling, because it cannot be the resource the ceiling protects. Found auditing the Tier-2 bound against §15.3. Clarifying prose; no contract or golden change. | none (prose) |  |
| 107 | 2026-07-25 | §13.1 (cross-ref §24.6 item 7) | **A revision floor survives a snapshot the Companion can no longer validate.** §13.1 requires a Companion to "retain each tombstone revision floor until pairing revocation" and not reclaim it "merely to admit a reused or new ID" — both phrasings about admitting NEW state, neither contemplating that a Companion may fail to re-accept state it already holds. It reliably can: retained snapshots are re-validated on restart, and a validator legitimately tightens between versions (this document's own amendments #98-#105 tightened several), so a snapshot accepted by one build is rejected by the next. The natural implementation — drop what no longer validates — silently reclaims that surface's floor, and §24.6 item 7's update/remove/update race then resolves the wrong way: a delayed older `surface.update` is answered `applied` instead of `stale`, resurrecting superseded content. §13.1 now states that a floor also survives the Companion's own inability to present its snapshot: an unacceptable snapshot MUST NOT be presented or dispatched, but the surface's history MUST be retained at its recorded revision and reported as a tombstone so Emacs re-pushes. Found auditing the contract-driven validator tightening (#106 batch) against §13.1. Clarifying prose closing a silent-data-resurrection hole; no contract or golden change. | none (prose) |  |
| 108 | 2026-07-25 | §4.5, §16.1, §7.1 | **Limits are receiver acceptance bounds, and node nesting gets its own — because host ENCODERS cap below what receivers accept.** §4.5's 64-container figure is a receiver bound, and §16.1 gave a node tree no depth budget of its own, so the only depth rule a sender had was the receiver's. That is unusable as a sender budget: a node level costs about two JSON containers, and host encoders cap independently and lower — Emacs's `json-serialize` stops at 50 containers (`src/json.c:566`, no runtime override, unchanged on master), i.e. about 24 node levels, so a document nested to the JSON limit is UNEMITTABLE by a conforming Emacs endpoint. Worse, the failure is silent in the wrong direction: in core `jsonrpc.el` the reply encode sits outside the dispatcher's `condition-case`, so a depth error while encoding a response escapes with no frame emitted, no close, and the peer's request never answered — a §7.1 violation reached by legal content. Three edits: §4.5 states the figures are acceptance bounds that do not license a sender to construct what its own encoder cannot emit (fail locally, never a partial frame); a new `max_node_depth` row bounds node nesting at 20 levels with §16.1 rejecting deeper documents `1201` / `data.reason: "node-depth"`; and §7.1 requires a responder that cannot serialize a computed result to answer `-32603` rather than leave the request outstanding. The constant is a design choice with margin, not an Emacs constant hard-coded into agnostic text: 2 envelope + 2×20 + leaf + wrapper = 44 ≤ 50. §25: restricts previously valid content (a 21-level document was legal). | contract `limits.fixed.max_node_depth: 20`; validate.py gains the node-depth walk it never had; validate.py green |  |
| 109 | 2026-07-25 | §4.5, §6.1, §6.2 (cross-ref §22.3) | **The header-section cap is a receiver rejection threshold, not a sender allowance; and an unclassifiable prefix owes a terminal outcome.** §4.5 published an 8,192-octet header section inside a MUST-enforce table, which reads as a budget a sender may spend — but the blessed receiver's real budget is **100 characters** (`jsonrpc.el:740-744`'s single bounded `search-forward-regexp`), and its failure mode is neither a close nor a diagnostic: `:745-746` sets `:waiting-for-new-message`, `delete-region` fires only on the completed-message branch, so the prefix is retained and every later octet re-runs the same failing search — a permanent silent wedge, measured on 30.1 (an 80-octet header accepted; 111/129-octet headers stall forever with the connection alive and nothing logged). The same shape reaches the body: the declared `Content-Length` is recorded with no upper bound, so an oversized declaration plus a slow dribble grows the buffer without limit, which §22.3's "MUST apply transport backpressure before unbounded memory growth" already forbids but no framing rule enforced. §4.5 now names a 128-octet figure a sender may rely on and scopes acceptance of the full cap (REQUIRED Companion / RECOMMENDED Emacs); §6.1 restricts the `android-loopback-tcp` header section to `Content-Length` alone and requires any future profile permitting more to define its syntax and budget; §6.2 requires a terminal outcome once an unclassifiable header prefix reaches the cap and bounds accumulation for a single incomplete frame at `max_frame_bytes` plus that cap, for every role. Complements #91, which bounded the delegation clause in principle; this bounds the two concrete accumulations. | contract `limits.fixed.max_send_header_bytes: 128`; validate.py green |  |
| 110 | 2026-07-25 | §6.1, §6.2 | **Body accounting is in transmitted octets, and a delegate that loses octet identity is a frame-injection primitive.** §6.2's two anti-injection rules — read exactly the declared octets, never resynchronize by scanning body bytes — presuppose a property the document never stated: that accounting is in TRANSMITTED octets. On the delegation path it does not hold. Emacs's UTF-8 decoder never errors on malformed input; `decode_coding_utf_8`'s `invalid_code` maps each bad octet to a raw eight-bit character that `BYTE8_STRING` writes as **two** internal octets, and `jsonrpc.el:751-758` measures the body with `position-bytes`, so a body containing *k* invalid octets is under-consumed by exactly *k* — after which `jsonrpc.el:740`'s unanchored forward search finds a `Content-Length` line inside the residue. Reproduced end to end on Emacs 30.1 over loopback: a single declared 162-octet frame whose body was 76×0xFF plus a complete attacker-authored inner frame delivered a notification the peer never framed. §6.1 now states body octets are counted and consumed as transmitted and that a length taken after any decoding step is not `Content-Length`; §6.2 conditions the delegation — the library's accounting SHOULD be octet-identical for arbitrary octets, it SHOULD close when declared and consumed counts disagree, and it MUST NOT locate a header section anywhere but immediately after a fully consumed preceding frame. Deliberately NOT promoted to REQUIRED for Emacs (that would breach amendment #34's scoping); the informative note records that octet identity is restorable without patching the library, by holding the transport and its accumulation buffer as uninterpreted octets. | none (prose) |  |
| 111 | 2026-07-25 | §7.1 | **An empty JSON object is a serializer duty, not a language default.** §7.1 requires `params: {}` / `result: {}` and forbids `null` as a generic success result, but a host serializer may map its language's empty/nil value to `null` rather than `{}` — core `jsonrpc.el` splices `:params params` unconditionally and `json-serialize` tests the null-object identity before the empty test, so `(jsonrpc-notify conn 'session.ready nil)` puts `"params":null` on the wire, contradicting this repository's own `goldens/wire/04-empty-params.bin`. Amendment #91 already moved the disclaimer from "no sender obligation in Section 6.1" to "in this document", which places §7.1 on the binding side; this adds the informative note naming the trap and its discharge (encode `{}` with a value the serializer maps to `{}` — an empty hash table, not the language's nil). Editorial; the golden already pins the correct wire form. | none (prose) |  |
| 112 | 2026-07-25 | §7.1, §7.5, §10.3 | **Requester abandonment is defined, and a replay always concludes.** §7.5 made `rpc.cancel` a MAY, §7.2's "concluded" (the precondition for ID reuse) was undefined for LOCAL abandonment, and §18.1's completion list contained no entry for requester silence — so a requester could walk away from a request the responder is obliged to keep outstanding forever, and the blessed library does exactly that: `jsonrpc-default-request-timeout` is 10 seconds, its expiry deletes the continuation, writes NOTHING to the peer, and drops the eventual real answer (the author's own TODO at that line notes the missing cancellation). The concrete loss: a `dialog.show` whose `capture_fields` carries a password is answered when the user submits, by which time the requester has silently dropped it — the credential is transmitted, erased at the source under §14.6, and discarded at the destination. §7.1 now states EBP defines no protocol-level deadline, sets a 60-second floor (SHOULD be none) for the user-paced methods §18.1 and §20.2 keep outstanding, and requires a requester that stops waiting to send `rpc.cancel` before treating the request as concluded, to ignore a later response, and never to reuse the ID; §7.5 makes that MUST explicit. Separately §10.3 step 4's "wait for it to conclude" defined conclusion ONLY for a replay returning `remaining > 0`, leaving a replay that errors (`1600`, `1301`) or is abandoned with no defined outcome and no bound on a stalled `SYNCING` — while a 256-record backlog at one round trip each trivially exceeds 10 seconds. §10.3 now concludes on any result, any error, or local abandonment, always proceeding to `session.ready` with FIFO preserved and no second replay before `READY`. | none (prose) |  |
| 113 | 2026-07-25 | §9.2, §9.3 | **"Cryptographically random" and "constant time" get checkable definitions.** §9 demanded CSPRNG-derived values in five places and nowhere said what disqualifies a generator; §26 cited no randomness reference; and nothing in §24.6 or the goldens (fixed nonces by design) could distinguish a conforming generator from a non-conforming one. The host makes the trap concrete: Emacs's `random` is seeded from a 32-bit value and its own manual de-advertises it for this use while naming no replacement, so the unconditional cryptographic route is indirect. §9.2 now requires a generator whose future outputs remain unpredictable to a party that has observed any number of previous outputs, and disqualifies one seeded from under 128 bits or a default pseudo-random facility not documented as cryptographic. §9.3's "compare in constant time" was the only occurrence of the term and was undefined — and `git grep` finds NO constant-time helper anywhere in Emacs 30.1, so the duty would be discharged by `string=`, which early-exits, which in turn nullifies amendment #79's equal-work path on the unknown-identity branch. §9.3 now states the property operationally (examine every position of both fixed-length proofs, combine differences, test once; never return at the first difference), applies it to §9.2's unknown-pairing-ID path, and explicitly scopes it to the endpoint's own program rather than requiring the impossible elimination of runtime and microarchitectural variation. | none (prose) |  |
| 114 | 2026-07-25 | §17.2, §17.5, §14.3, §20.3 | **The `data:` base64 profile is pinned, and `on_point_tap` injects the index §17.5 already computes.** Two independent one-byte-two-outcomes holes. (a) §17.2 required the Companion to "validate the media type and decoded bytes" of a `data:image/*` URL but never stated the base64 profile — so a strict decoder rejects the ENTIRE surface with `1201` (§16.1 forbids normalizing) where a MIME-tolerant one renders, and Emacs's `base64-encode-string` line-wraps at 76 columns unless its optional argument is passed, making the divergence a default-argument mistake away. Now: standard [RFC4648] alphabet, padding required, whitespace forbidden, with an OPTIONAL line-feed tolerance a sender MUST NOT rely on (so the mistake degrades to a rendered image rather than a lost snapshot), applied to §20.3's `icon_png` too. (b) §14.3 gave `on_point_tap` only `value`, while every peer index-bearing hook injects an index — and amendment #55 had already made §17.5 resolve the tap to an ordinal index in the first series and then discard it. Value-identity is unsound: §17.5 imposes no distinctness on authored points, so `[{x:0,y:5},{x:0,y:5}]` is legal and no comparator can disambiguate, and §4.3 plus insignificant member order let a conforming Companion echo the point permuted or renumbered. `index` is now injected beside `value`, with the echoed point demoted to convenience data. §25: restricts previously valid content (an Emacs-authored `args.index` on that hook now conflicts). | contract `actions.injections.on_point_tap` += `index`; new `image_data_encoding`; validate.py green |  |
| 115 | 2026-07-25 | §19.3, §19 preamble | **A resync after a REFUSED splice must not install the refusal as the shared baseline.** §19.3 requires the receiver of an invalid splice to mark the session stale and resynchronize — and §19.4 makes resynchronization adopt the COMPANION's state. For a view that merely fell out of sequence that is right. For an endpoint whose authoritative document REFUSED the splice — a read-only region, a locked buffer, a rejecting mode hook — it is exactly backwards: the document said no, the recovery adopts the Companion's text, and the refusal silently becomes the new truth, with no rule anywhere restoring the authoritative content. §19.3 now requires such an endpoint, after the resync completes, to issue at the new `seq: 0` the `edit.apply` that restores its authoritative text, and forbids suppressing or overriding a document's own write protection to apply a received splice. The preamble adds the missing representational advice: sub-document write protection has no wire representation, so a document not writable in full SHOULD be presented `read_only: true`, and a Companion is entitled to treat an editor it was not told is read-only as fully writable. | none (prose) |  |
| 116 | 2026-07-25 | §17.7 (cross-ref §19) | **`promote` and `demote` are made genuinely inverse, and the outline forms require their following space.** §17.7 asserted the two `line` ops were inverse "steps", but against real outline grammar they were not, and the corrupted intermediate flows to Emacs through §19. Traced by hand: `"  * sub item"` is a legal indented bullet; promote's de-indent branch stripped two spaces to yield `"* sub item"`, which at column 0 followed by a space is a HEADING; demote then produced `"** sub item"`. `promote∘demote` was not the identity. Two further holes: `"+ item"` was a legal bullet promote de-indented but demote (which enumerated only `-`) could never restore, and the missing space requirement made promote corrupt emphasis — `"*bold* text"` became `"**bold* text"`. §17.7 now states both `*` forms require a FOLLOWING SPACE, protects a `*`-prefixed line from being de-indented to column 0, adds `+` and `*` to the unordered markers (each also marker-plus-space, so emphasis is not misread as a bullet), gives any other indented line a re-indent so promote's de-indent has an inverse, and states the inverse property as a MUST. Written as literal-text rules with no dependency on any particular outline language. **This row also records the §17.7 rewrite that shipped inside the amendment 112-115 commit without a changelog row** — §25 requires every normative change be classified, and it was not; the rewrite is hereby classified as restricting previously valid behavior. | none (contract `toolbar.line_ops` vocabulary unaffected) |  |
| 117 | 2026-07-25 | §7.1 (cross-ref §18.1) | **The serialization-failure rule carves out the methods whose own section keeps the request outstanding.** Amendment #108 added "a responder that computes a result but cannot serialize the response body MUST answer `-32603`; it MUST NOT leave the request unanswered" — which contradicts §18.1, unchanged, which for an oversized `dialog.submit` result requires the Companion to "MUST NOT write any part of the response or complete the dialog" and to keep the dialog outstanding so the user can shorten the input. Both are MUSTs over the same event and one of them had to be unsatisfiable; a conforming implementation of §18.1 (which this repository has) was made non-conforming by #108 the moment it landed. §7.1 now carves out a method whose own section requires the request to stay outstanding for that fault, names §18.1 as the one such case, explains why (answering `-32603` would discard input the user has not been given a chance to correct), and requires any section claiming the exception to say so explicitly and define how the request eventually concludes. Found auditing amendment #108 against the sections it touched. | none (prose) |  |
| 118 | 2026-07-25 | §17.2 | **A malformed `data:` payload is content-invalid at accept time, not a blank frame at render.** Amendment #114 pinned the base64 profile but left the *timing* of the check to the reader, and the reference Companion read it the lazy way: `isValidImageUrl` inspected only the `data:<media-type>;base64` header, so a payload outside the alphabet — or with its padding dropped, or line-wrapped by an encoder — passed validation, `surface.update` answered `applied`, and §13.1's revision floor advanced past a document that can never render. Emacs is then told the surface is live, holds no error to bind to the node, and the user sees a blank frame with nothing anywhere to explain it. §17.2 now states that the payload's alphabet, padding, and whitespace-freedom are part of the URI form, so a non-conforming payload MUST be rejected as content-invalid when the document is validated, on the same terms as an unadvertised URI form; and that a Companion MUST NOT accept such a document and report the failure only at load time. The decoded byte count and pixel limits stay load-time concerns, because those genuinely need the decode. The form check is a scan of the payload with no decode and no allocation, so the split costs nothing. | none (`image_data_encoding` already pinned by #114) |  |
| 119 | 2026-07-25 | §24.5, §24.6 | **A Golden says which role its expectation binds.** §24.5's identity list had no role bullet, so `goldens/wire/manifest.json` carried exactly one `expect_error` per fixture — while §6.2 scopes several receiver duties BY ROLE and §24.2 restates that scoping for Emacs. Measured on GNU Emacs 30.1 against this repository's own fixture bytes, five expectations are unproducible by a conforming Emacs endpoint that elects the delegation clause: `19-duplicate-members` is silently accepted first-wins under `plist-get` (the same parse with the default hash-table object type is LAST-wins, which sharpens it), `21-deep-nesting` is silently accepted (`src/json.c:767`, `available_depth = 10000`), `16`/`17` are dropped with a warning and no `-32700` frame (`jsonrpc.el:766-770`), and `18-batch-array` raises an internal `(wrong-type-argument plistp [1 2])` with no frame at all. A suite author reading only §24.5 would score a conforming endpoint as failing, or — worse — relax the vector for both roles. §24.5 now names the role dimension, states that an absent list means both roles, and requires an excused role to be held to a bounded terminal reaction (stream synchronized or closed, no application effect) rather than skipped; §24.6's preamble points at the per-role scoping so the list is read correctly. | `goldens/wire/manifest.json` gains `roles` on the eight fixtures whose expectation is Companion-only (11, 12, 14, 16, 17, 18, 19, 21); `validate.py` validates the key and enforces that the Companion is never the excused role (§24.1 grants it no delegation); the Kotlin runner asserts the same; the elisp runner reads it and declares `ebp-test--claims-strict-framing` — this endpoint does NOT delegate, so it stays held to the Companion-strength expectation. | goldens/wire/manifest.json |
| 120 | 2026-07-25 | §4.3, §24.6 | **A conformance vector for equality-by-value, because same-spelling operands cannot produce one.** §4.3 is precise and complete — `1` = `1.0` = `1e0`, `-0` = `0`, member order irrelevant — and a conforming comparator is ten lines. What was missing is a TEST: nothing in §24.6's items or in `goldens/` distinguished a §4.3-conforming endpoint from one that delegates to a host primitive, and every host primitive within reach gets it wrong in a different direction. `emacs-30.1:src/fns.c` `internal_equal` opens with `if (XTYPE (o1) != XTYPE (o2)) return false;` and `same_float` compares REPRESENTATIONS ("unlike `==`, it returns true if X and Y are the same NaN"), so `(equal 1 1.0)`, `(equal -0.0 0.0)`, `(seq-contains-p [1 2 3] 2.0)` are all nil — meaning `equal`, `eql`, `member`, `assoc`, `seq-uniq`, `delete-dups` and equal-tested hash tables violate §4.3 on the exact values §13.6, §14.6, and §17.4 require compared. Python fails the other way: `bool` subclasses `int`, so `==` equates `true` and `1`. §24.6 gains item 15 (differing spellings through draft reconciliation, `state.changed` reconciliation, and enum-option distinctness and selection); §4.3 gains an informative note that the type tag must gate before the value does. | three `widgets.golden` vectors valid ONLY under §4.3 — a discrete slider whose `value` is `2.0` against `values:[1,2,4]`, an `enum_list` selecting `2.0` from integer options, and a slider whose `value` is `-0.0` against `values:[-1,0,1]`; `validate.py` gains `spec_equal` plus the enum-distinctness and discrete-slider checks that use it, and a `check_equality_semantics` self-test naming both host traps. | goldens/widgets.golden 72-74 |
| 121 | 2026-07-25 | §21.5 | **Durability covers what was observed, not every transition that happened.** §21.1's "accepted registrations MUST persist across Companion and device restarts", read with §15's durable-queue vocabulary, invites an author to model `power`/`screen`/`network`/`battery.level` + `queue` + `ttl_s` as guaranteed eventual delivery of every platform edge — while §21.5 already means an edge occurring while the Companion was not executing is absorbed into the baseline re-established at restore and can never fire. §21.8's `trigger_unavailable` covers only permission revocation, so nothing on the wire distinguished "no transitions occurred" from "the Companion was dead for six hours", and an Emacs endpoint had no way to learn an observation gap existed. The spec already makes the concession explicitly for `time` (missed intervals coalesced into one) and never made it for the transition types. §21.5 now scopes `queue`/`wake` durability to occurrences the Companion observed and admitted under §21.2, states that an unobserved transition is absorbed by the restored baseline, and forbids presenting a transition-driven type as a complete event log. Platform grounding: `emacs-30.1:doc/emacs/android.texi:474-477` "Application processes are treated as disposable entities by the system"; :498-504 "it is not guaranteed that the system will not kill Emacs… many manufacturers institute additional restrictions." | none | |
| 122 | 2026-07-25 | §20.1 (cross-ref §8, §21.8) | **A blocking permission identifier must resolve against the snapshot beside it.** `trigger_unavailable` is the sole wire channel telling Emacs a supported trigger cannot arm, but nothing required its values to appear as keys of `device.permissions`, to be `false` there, to match `data.permission` on `1002 cap-permission`, or to stay stable across Companion upgrades. Two Companions could both conform while an Emacs-side remediation affordance key-misses under both. The spelling problem is real on the target platform: `java/AndroidManifest.xml.in` declares fully namespaced grants (`android.permission.POST_NOTIFICATIONS`, `RECEIVE_SMS`, `READ_CALENDAR`, …) and `doc/emacs/android.texi:535`/`:691` reproduce that spelling, while this document's own §20.1 example writes `{"post_notifications": true}` — a third spelling. §20.1 now requires every identifier in a `trigger_unavailable` array to appear as a `permissions` key with the value `false` when the welcome is sent, forbids reporting a blocking identifier absent from the snapshot, requires the same identifier for the same platform grant across sessions and Companion upgrades for a pairing identity, and SHOULDs the same identifier in `data.permission`. The vocabulary stays deliberately unprojected — the identifiers are opaque per §4.4 and belong to the host platform; the requirement is that the three places an endpoint meets one AGREE, not that any particular spelling be used. A future contract projector should not try to enumerate them. | none (no contract vocabulary; no golden exercises a device report) | |
| 123 | 2026-07-25 | §23.2 | **Interpretation is a sink too, not just execution.** §23.2's five bullets — eval, untrusted-input execution, ambient dispatch, shell, deserialization — are all execution sinks. None covered using a received string as the FORMAT, template, query, or pattern that the host interprets, rather than as an inert argument to one; and that is precisely the idiom §7.3 invites by requiring unknown methods and invalid params to be logged. `emacs-30.1:src/editfns.c` `styled_format` signals on a stray `%` (`:3598` "Format string ends in middle of format specifier", `:3777` "Invalid format operation %%%c") and caps no field width, while `lisp/jsonrpc.el:320` dispatches notifications BARE — `(funcall ndispatcher conn (intern method) params)` with no `condition-case`, in contrast to the request arm's — so such a signal escapes message dispatch entirely. Honest scoping: the bare-template idiom yields a signal, not the 100 MB allocation (that needs a call that also supplies an argument), and the blessed library does not commit the bug. This is editorial hardening of an enumeration, phrased for any host language. §24.6 item 4 is the natural place for a suite to send a `%`-bearing unknown method name. | none | |
| 124 | 2026-07-26 | §7.4, §22.2, §22.3 | **A half-written frame has a name, and the sender's call stack is not the wire.** RESEARCH-A8 §1 measured the state the load-management vocabulary could not describe: a blocking `process-send-string` on GNU Emacs 30.1 spins in `wait_reading_process_output`, which runs `timer_check`, and `jsonrpc.el` dispatches inbound messages from timers — so handlers run INSIDE the sender's own half-written frame (reproduced 6/6; nesting unbounded, completion LIFO, outbound bytes strictly FIFO via `write_queue`, so the wire stays intact while the sender's stack does not). The scenario is reachable in ordinary operation because the reference Companion's single reader thread validates synchronously and writes from that same thread — its §7.4 in-order guarantee is what creates the sender-side exposure — and the same measurement showed 64 MiB irrevocably in kernel and `write_queue` buffers with the sending call not returned: bytes neither "waiting locally to be sent" nor deliverable-to-caller as a conclusion, a third state §22.2's two-way partition had no name for. Three edits. §22.2: the boundary between unsent and transmitted is the HANDOFF to the transport (the irrevocable moment on any real host); a sender MAY conflate only before the handoff, and a message *in transmission* is already transmitted for every rule in the section even while the sending call has not returned. §7.4: a send is not atomic w.r.t. the sender's own state — an endpoint MAY process inbound messages during its own send (on some hosts that processing is what lets the peer drain and the send complete, per A8 §1.5 where suppressing it converts overload into deadlock), MUST NOT assume its send concluded before inbound dispatch runs, and state a send claimed stays claimed while the send is in progress. §22.3: re-entrant dispatch depth joins the MUST-bound resources — under the LIFO nesting it is the one §22.3 resource that grows on the sender's own stack, and no existing bullet bounded it. | none (prose; no contract vocabulary, no golden — the vectors land in the endpoint suites under §24.6 item 14) | |
| 125 | 2026-07-26 | §22.3 (precedent §23.5) | **An unbounded delegate queue is the endpoint's own unbounded queue.** §22.3 names four queues every endpoint MUST bound, but §6.2's delegation clause ("accepting that library's tolerances") says nothing about a delegate's QUEUE BOUNDS, so the paragraph licensing a rented transport silently licensed renting its unboundedness — the root-cause shape the Kotlin-core review already attributed 13 of 31 findings to. Concretely measured on the blessed Emacs library (RESEARCH-A8 §7): under `jsonrpc.el` at emacs-30.1 the inbound frame queue lives in the process buffer, the parsed-message queue in `jsonrpc-mqueue` drained onto `timer-list` (`jsonrpc.el:779`), and the outstanding-request table in the connection's continuations — none with any cap, so an Emacs endpoint could claim §22.3 conformance while every queue the section names grew without bound in code it merely delegated to. §23.5 already established the pattern for this exact hole (its interning rule ends "This obligation is not relaxed by Section 6.2"). §22.3's first paragraph now carries the parallel sentence: the bounding obligations are not relaxed by §6.2 — a queue inside a delegated transport or dispatch library is the endpoint's own queue, and its growth is the endpoint's own growth. Restricting only in the sense of closing a loophole reading; the four bounds were already MUSTs of the endpoint. | none (prose) | |
| 126 | 2026-07-27 | — (no spec text) | **The syntax-role registry reconciled by deletion, not registration.** The reference Companion read two roles — `meta` and `paren` — that appear in neither this document nor `contract.json`, which SPEC 18.4's "unknown syntax roles MUST be ignored" forbids; meanwhile five registered roles (`variable`, `type`, `operator`, `preprocessor`, `tag`) were implemented nowhere. Two repairs were available: register the two extras, or delete them. Option B was ratified: `SyntaxHighlight.kt` now drives org tags from the registered `tag` role and meta/table lines from `preprocessor`, and the paren rainbow is deliberately STATIC — a Companion-local presentation choice with no wire role, which is why registering one was the wrong instinct. The registry is unchanged, so this row records a decision rather than a text edit; it exists because the drift it closes was invisible to all three pinning rails until #127 gave them eyes. | none (Kotlin only; the elisp `:meta` duplicate emitter dies with it) | |
| 127 | 2026-07-27 | §18.4, `contract.json` | **The wire's richest payload gets its pinning rails.** `fg` — the only SyntaxStyle member Jetpacs ever emits — was absent from `contract.json` `field_types`; there was no `syntax_style` schema; `validate.py` read neither `theme_roles` nor `syntax_roles`, the only two top-level contract sections it ignored; and the single `theme.set` golden carried one syntax role. That combination is mechanically why the #126 drift survived three rungs: nothing anywhere could compare what the Companion read against what the registry named. §18.4 now projects SyntaxStyle into `contract.json` as `syntax_style` (optional `fg`, `bg`, `font_weight`, `italic`, `underline`), REQUIRES a conformance validator to check `colors` member names against `theme_roles`, `syntax` member names against `syntax_roles`, and every SyntaxStyle member against `syntax_style`, and REQUIRES the golden corpus to carry a `theme.set` frame exercising the complete `theme_roles` set and at least three distinct syntax roles. The same edit adds the missing prohibition the registry always implied: a Companion MUST NOT interpret one standard role as another, and ignoring a role it cannot express does not license substituting a different role's style for it — the precise thing reading `meta` for org tags did. Additive: no member is added to the wire, and every previously valid frame stays valid. | contract `syntax_style` + `syntax_roles`; validate.py theme walk; `theme.set` golden regenerated | |
| 128 | 2026-07-27 | §13.4 | **A surface may leave the multi-view shape and come back.** 13.4's three view-change conditions presuppose CONSECUTIVE multi-view snapshots. They say nothing about the transition through a single-root spec — which is exactly the path a chrome surface takes when its builder fails and it degrades to an error spec, and then recovers. The reference Companion resolved it (`SurfaceStore.kt`: not multi-view → clear) by a rule not derivable from the text, so two conforming Companions could disagree about whether a remembered view survives a degrade. §13.4 now states it: an accepted snapshot carrying no `views` clears any retained current view, so the next multi-view snapshot omitting `current_view` selects `initial_view`. Ratifies the reference behaviour; nothing previously conforming becomes non-conforming, because nothing was previously specified. | none | |
| 129 | 2026-07-27 | §10.2 | **The navigation channel stops being write-only.** `surfaces` reported `{revision, present}` and `view.switched` is `when_offline: "drop"`, so a local navigation performed while Emacs was disconnected was unrecoverable: at the §10.3 barrier Emacs knew the revision it had sent and nothing about where the user actually was. Implementations were left to invent a policy for the resulting drift, and one of them declared it "benign by design" — a policy filling a spec hole rather than following one. The welcome's `surfaces` entry MUST now carry `current_view` for a present `app:*` surface whose latest accepted snapshot is multi-view, and Emacs MAY name the already-shown view in an update as an explicit no-op. Additive to the welcome; the reference Companion already persisted the value. | contract welcome `surfaces` entry | |
| 130 | 2026-07-27 | §22.3, §24.6 (cross-ref §8, §18.1) | **Two different `1401`s were byte-identical, and only one of them meant the peer declined.** §18.1 MANDATES a `1401 overloaded` RESPONSE when `max_dialogs` would be exceeded, while §22.3 describes an identically shaped `1401` as a purely local sender-ceiling conclusion that MUST NOT reach the wire. No clause supplied a discriminator and none stated any receiver obligation on a received one, so an endpoint that treated its own refusal as a peer answer would pause its durable queue on a `blocked_by` the peer never sent — and an endpoint that treated a peer answer as its own refusal would retry a request the peer had explicitly declined, unboundedly. §22.3 now states that `1401` MAY be returned as a response where a method's registry entry lists it, that an endpoint MUST NOT conflate the two and MUST make a locally generated refusal distinguishable by the consuming code path, and that a received `1401` response MUST NOT be resent immediately — bounded backoff and a bounded consecutive-resend count are REQUIRED. §24.6 gains item 16. Nothing normatively breaks: both behaviours were already required, only the ambiguity is removed. | none (prose; the vector lands in the endpoint suites under §24.6 item 16) | |
| 131 | 2026-07-27 | §22.3 (cross-ref §14.4, §15.1) | **Durability becomes something the receiver can SEE.** §14.4 states flatly that returning `accepted` "merely because a volatile callback was scheduled is not conforming", but Emacs had no seam by which to tell a `drop` occurrence from a durable one — so a handler could not know whether concluding asynchronously was permitted, and the honest implementations were guessing. §15.1 already REQUIRES the Companion to persist a `queue` or `wake` event before every delivery attempt, including on an already-`READY` session; that fact simply was not observable on the wire. The Companion MUST now include `queued_at_ms` in the params of every `queue`/`wake` delivery and MUST omit it for a `drop` delivery, and Emacs MAY treat its presence as proof of a durable record. Promotes `queued_at_ms` from informational to constraining, so §12 rule 2 applies: a Companion may never omit it for a durable delivery. The reference Companion already conforms. | contract `event.action` params (`queued_at_ms` constraining) | |
| 132 | 2026-07-27 | §18.3 | **An invalid pie menu produced nothing, anywhere.** §18.3 says "Any authored conflict makes the menu invalid" and attaches no diagnostic duty; the only mandated `log.error` is the count limit. The reference Companion accordingly discarded a conflicting menu in total silence, so a Tier-1 author's first invalid menu yielded no report on either side of the wire — the author's own console said nothing and Emacs was never told. A Companion that discards a `pie_menu.show` for invalidity MUST now emit `log.error` with `1201`, `data.kind: "content-invalid"`, `data.reason: "pie-menu-invalid"`, and `data.path` naming the offending member. One new log path in the reference Companion; no sender behaviour changes. | none (existing `1201` class and `log.error` shape) | |
| 133 | 2026-07-27 | §18.6 | **A reminder is neither a surface nor a dialog, so it cannot capture fields.** §14.1 makes `capture_fields` valid only inside a surface or dialog containing every named node, and §18.5 already carries the twin prohibition for notification actions — but §18.6 said nothing, so the two endpoints disagreed: the elisp rejected it (citing the wrong section) and the reference Companion accepted it, never inspecting the member. A reminder's `on_tap` MUST NOT carry `capture_fields`; a `reminders.set` containing one is invalid and the Companion MUST reject the ENTIRE request with `1201 content-invalid` naming `on_tap.capture_fields` — whole-request rejection, matching §23.5's "MUST reject excessive content atomically and MUST NOT partially execute a rejected object". One new check in `validateReminder`; no conforming sender is affected. | none (existing `1201` class) | |
| 134 | 2026-07-27 | §13.1 | **Owner names in the `app` namespace need a claimant discipline.** The document defined Surface ID syntax and the `app:` namespace but stated no policy for who may claim `app:<owner>` when several independently distributed packages register inside one Emacs. Because §13.5 persists snapshots keyed by Surface ID, a collision is a wire-visible data hazard — the second claimant inherits the first's persisted snapshots and revision history — not a local naming inconvenience. An implementation hosting more than one independently distributed application MUST now provide a reservation discipline, MUST document any prefix it reserves for itself, and MUST report a second distinct claimant as an error rather than silently replacing the first; re-registration by the same claimant (interactive development) is explicitly not a second claimant. Cheap now, expensive once third-party applications ship. | none (prose) | |
| 135 | 2026-07-27 | `contract.json` | **The rung's central wire shape was unrepresented in the contract.** `surface_spec_variants` registered only `tile`, while `views`, `initial_view` and `current_view` appeared nowhere in `contract.json` and `validate.py` never read the section — so a validator generated from the contract accepted a multi-view object on a `widget:*` or `notification:*` spec, which §13.4 requires be rejected `1201`. The section MUST now register every namespace variant defined in §13.4, including the `app` multi-view object with `views` (required), `initial_view` (required), and `current_view` (optional, valid only on a request). Additive; `validate.py` gains a variant check. | contract `surface_spec_variants.app`; validate.py variant check | |
| 136 | 2026-07-27 | §23.3 | **The informative note undercut the normative rule it illustrated.** §23.3's normative text already required disabling or redacting every host-library facility that records raw frame bodies, but the note beneath it told implementers that constructing the connection with `:events-buffer-config '(:size 0)` and removing the hook "satisfies this rule" — and an implementation that followed the note was non-conforming, which is exactly what happened. At `jsonrpc.el` 1.0.25 the decode-failure path is `(jsonrpc--warn "Invalid JSON: %s %s" … (buffer-string))`, and `jsonrpc--warn` reaches `*Messages*` through `jsonrpc--message` BEFORE it reaches `display-warning`, so the raw body — a volatile password value, an authentication proof — survives both settings and lands in two sinks this section names. Reproduced with a planted secret. The note now states what the two settings do and do not cover. Editorial: the normative requirement is unchanged, but an implementation that stopped where the note said to stop must add the missing redaction. | none (the vector lands under §24.6 item 12) | |
| 137 | 2026-07-27 | §23.2 | **A deliberately exposed grammar is still interpretation.** §23.2's fourth bullet already forbids using a received string as a pattern the host interprets, and names regular expressions outright — but its informative note frames the hazard as the one "an implementation reaches for by accident", so an implementation that *designs* a peer-facing search grammar reads itself as outside the rule. It is not: the receiver still hands peer text to its own engine, and a designed grammar is a wider door than an accidental one because it is documented and reachable by intent. The bullet now states the prohibition is not lifted by the interpretation being intentional, and requires a receiver exposing a search/filter/query grammar either to quote the peer's argument or to evaluate it through a cost-bounded matcher. Measured on the reference Emacs endpoint: a 12-byte pattern of the shape `\(a*\)*b` against a 36-character body did not return, and — the part worth ratifying — an in-process `with-timeout` wrapped around the call NEVER FIRED, because a C-level regexp match does not yield to timers. An in-process timeout is therefore not a conforming mitigation; the bound must be established before the pattern reaches the engine. Restricting only in the sense of closing a loophole reading: the fourth bullet's MUST NOT already covered this case. | none (prose) | |
| 138 | 2026-07-27 | §23.5 (cross-ref §4.5) | **Interpretation cost is bounded independently of message size.** §23.5's second paragraph bounds what a peer's *names* can grow at decode time, and §4.5 bounds a message's size, depth, and identifier lengths — but nothing bounded what a receiver spends *interpreting* a message that is well inside every size limit. The gap is exploitable in both directions measured on the reference endpoint: a free-text query string within the 4 MiB body limit expands to ~1.4M filter clauses evaluated per entry (5,000 clauses = 9.8 s, 20,000 = 31.0 s, extrapolating to tens of minutes of uninterruptible work), and an evaluation memo keyed on the peer's own query text grows for the process lifetime with no eviction. §23.5 now requires the work performed by evaluating peer-supplied structure to be bounded on its own terms, forbids relying on §4.5's limits to do it, and extends the existing bounding-and-reclamation obligation to any table retained under a peer-supplied key, including a result cache. Additive: no previously conformant receiver becomes non-conformant by bounding work it was already obliged to survive. | none (prose) | |
| 139 | 2026-07-27 | §23.2 (precedent §42, cross-ref §18.1, §23.1) | **Naming the untrusted sources, and pinning what a trust decision is.** §23.2's second bullet lists the input channels whose text MUST NOT execute without "a separate explicit trust decision", but the list predates dialog `capture_fields` (§18.1) and says nothing about text arriving from a platform share or intent — the two channels by which a modern companion most often hands an editor a string that a local template facility will then expand. Both are added. Separately, "separate explicit trust decision" was never defined, so a standing setting could satisfy it by construction. Following amendment #42's per-combination model, the decision is now REQUIRED to be per invocation, to identify what will be interpreted and with what authority, and to default to declining; pairing, a capability grant, a configuration toggle, and the user having chosen this application as a share destination are each explicitly insufficient alone — they establish that the user meant to send the text here, not that they meant it to run. The amendment also blesses the cheaper alternative: an implementation MAY neutralize the text instead, which requires no trust decision and is RECOMMENDED where the interpreting capability is incidental to the feature. A non-normative note records the shape this exists for — peer-supplied values substituted into a user-controlled template *before* the template is expanded, which silently promotes the value to the template's authority. Restricting for an implementation that relied on a standing toggle; the underlying MUST NOT is unchanged. | none (prose) | |
| 142 | 2026-07-31 | §4.5 (cross-ref §14.1, §15.1) | **The capture budget inequality, stated.** §4.5's REQUIRED minimums allow `max_capture_fields` (64) × `max_field_bytes` (65536) = 4194304 bytes of captured input — sixteen times `max_event_bytes`' floor (262144) and equal to the whole frame cap — and no rule says what happens when a legal ActionDescriptor's capture assembles an `event.action.params` that exceeds `max_event_bytes`. §4.5 already states exactly this kind of inequality for event-vs-frame ("An `event.action` sender MUST encode the fixed envelope outside `params` in no more than 256 UTF-8 octets. Together with the upper bound above, this guarantees that an allowed event also fits `max_frame_bytes`"); the capture dimension was left out. The section now states that the per-dimension caps do not compose into an admission guarantee: `max_event_bytes` is the binding budget at capture time, and a capture whose assembled params would exceed it MUST NOT create the event, MUST NOT truncate any captured value, and MUST present a visible diagnostic exactly as §15.1 requires for queue-capacity exhaustion. Clarifying prose resolving an undefined receiver duty; no limit value changes. | none (prose) | Caleb Christensen |
| 144 | 2026-07-31 | §15.2 (cross-ref §14.1, §21.1, §21.2) | **The dedupe key space, stated: flat per pairing identity.** §14.1 types `dedupe` as an identifier "scoped to the pairing identity" and §15.2 replaces on "the same key and pairing identity" — a flat space, unqualified by action, surface, or origin. Trigger firings with a `queue`/`wake` policy enter the same durable queue and the same dedupe rule (§21.2's pipeline invokes "the durable-queue `dedupe` rule before delivery"), so an action's key and a trigger's key collide and silently annihilate each other's intent. The spec's own examples then disagree about whose job collision avoidance is: §14.1's example encodes the replaced intent's identity in the key (`heading:123:todo`) while §21.1's is bare (`battery-low`), and no sentence tells an author that a bare key is a pairing-global replacement channel. §15.2 now states the flatness explicitly — the space is shared across every action name, surface, and trigger of the pairing identity — and requires a sender that does not intend cross-intent replacement to author keys encoding the replaced intent's identity; §21.1's example key becomes `battery.level:low` to match §14.1's discipline. Clarifying prose; the operative replacement rule is unchanged, so no previously valid frame changes meaning — including `frames.golden`/`widgets.golden`, which carry `"dedupe":"battery-low"` and stay conformant (the new MUST is sender-intent discipline, not a validity rule). | none (prose; goldens keep their bare key legally — verified) | Caleb Christensen |
| 148 | 2026-07-31 | §13.2, §5.2 (cross-ref §10.3, §24.2) | **Mid-session results absorb like welcome floors; a terminated session's requests conclude before the welcome is built.** Emacs's duty to absorb Companion revision floors existed only at the synchronization barrier (§10.3 step 1; §24.2's "absorption of Companion revision/tombstone floors") — yet every `applied`/`stale` result also reports "the Companion's current revision floor" (§13.2), and no rule made Emacs adopt it. An Emacs whose revision store regressed (a restored backup, a lost file) could then author below the floor indefinitely: every update draws `stale`, nothing requires the sender to learn, and the surface is silently frozen until the next reconnect. §13.2 now makes any result carrying a revision a normative absorption point — Emacs MUST treat it exactly as a welcome floor: persist it and never author that surface at or below it. Separately, §5.2 ordered termination before the new session enters `SYNCING` but never said what happens to the old session's requests still being processed while the welcome snapshot is assembled — leaving welcome-snapshot mutability open: a terminated session's `surface.update`, applied after the snapshot was built, makes the welcome lie. §5.2 now requires the Companion to conclude every pending inbound request of the terminated session — fully applied with its response committed to the old transport, or discarded without effect — before assembling the new session's welcome, and forbids a terminated session's request from altering any state the welcome reports. Clarifying prose pinning duties both endpoints' reference implementations already satisfy in the common path. | none (prose) | Caleb Christensen |
| 150 | 2026-07-31 | §24.4 (cross-ref §25) | **What the projection cannot express, stated — and enforcement claims made falsifiable.** §24.4 defines the contract projection's required contents and already warns that "a property-name inventory without JSON types is not a sufficient contract projection" — but never stated the converse limit: ordering, atomicity, clock, durability, and session-state semantics are outside any schema projection's expressive range, so contract conformance alone gates only shapes, never behavior. The gap is not hypothetical: this repository's own sync tool covers two of the contract's thirty-one top-level registries while being cited as "binding prose↔contract", the exact stated-vs-actual-enforcement failure the source audit documents three times over. §24.4 now states the limit and imposes the falsifiability duty: a claim that a tool or artifact enforces part of this document MUST name the tool and the exact scope it checks; a conformance claim broader than its named scope is a process defect under §25. This is the documentation-side half of #151's standing rule. Clarifying prose. | none (prose) | Caleb Christensen |
| 151 | 2026-07-31 | §25 (cross-ref §24.4; precedent #81, #107) | **Two standing amendment duties.** Three times in this spec's history an amendment closed a symptom and deepened the structure underneath: #81 pinned the queue's stamping clock and left the ratchet's forward direction unbounded (draft #140); #107 hardened floor retention and left the floor space non-reclaimable (draft #141); plain revision monotonicity left the revision space exhaustible with no rule (draft #149). Each survivor is the same genre: a monotonically consumed resource whose exhaustion nobody was made to state. §25 now imposes the duty at the door: an amendment that introduces or modifies a monotonically consumed resource — an identifier space, a revision or sequence space, a retained floor, a nonce or dedupe-retention window, a counter — MUST state that resource's exhaustion behavior and reclamation path, or state explicitly that none exists and why that is acceptable. And its sibling, the enforcement-naming duty: an amendment that claims a property is enforced MUST name the enforcing tool and the scope of its enforcement (§24.4 imposes the same on conformance claims generally). Process prose; changes no wire behavior. | none (prose) | Caleb Christensen |
